# Korea Discount - Value-Up Compliance Study
# Install: pip install -r requirements.txt
# Note: eikon requires a Refinitiv Eikon/Workspace terminal and REFINITIV_APP_KEY in .env

# Primary data pull — LSEG Workspace desktop (src/00_build_universe.py, src/01_bloomberg_pull.py)
# Requires LSEG Workspace desktop app to be open and logged in at runtime.
lseg-data>=2.0.0

# Public-API fallback (no terminal required — see src/00b_* and src/01b_*)
yfinance>=0.2.40
finance-datareader>=0.9.50

pandas>=2.2.1
numpy>=1.26.4
scipy>=1.12.0
statsmodels>=0.14.4
python-dotenv>=1.0.0
matplotlib>=3.8.3
seaborn>=0.13.2
stargazer>=0.0.7
