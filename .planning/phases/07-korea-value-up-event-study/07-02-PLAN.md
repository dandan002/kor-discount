---
phase: 07-korea-value-up-event-study
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/analysis/event_study_core.py
  - src/analysis/korea_event_study.py
  - tests/test_phase7.py
  - output/figures/figure_korea_event_study.pdf
  - output/tables/korea_event_study_car.csv
  - output/tables/table_korea_event_study_coefs.tex
autonomous: true
requirements:
  - KEVNT-01
  - KEVNT-02
  - KEVNT-03

must_haves:
  truths:
    - "Running `python src/analysis/korea_event_study.py` produces Korea CAR outputs from the canonical panel"
    - "The Korea implementation uses the primary Korea policy from Phase 6 rather than reassigning global Japan defaults"
    - "Korea output windows respect the primary policy `max_post_months` limit and handle clustered 2024 overlap explicitly"
    - "The Korea figure/table/CSV use the same presentation style as the Japan event-study artifacts while keeping distinct filenames"
  artifacts:
    - path: "src/analysis/korea_event_study.py"
      provides: "Standalone Korea event-study script"
    - path: "output/tables/korea_event_study_car.csv"
      provides: "Machine-readable Korea cohort x month CAR output"
    - path: "output/tables/table_korea_event_study_coefs.tex"
      provides: "Paper-ready Korea event-study coefficient/CAR table"
    - path: "output/figures/figure_korea_event_study.pdf"
      provides: "Korea event-study CAR figure"
  key_links:
    - from: "src/analysis/korea_event_study.py"
      to: "config.KOREA_EVENT_SET_POLICY"
      via: "primary policy drives dates, labels, and max post window"
      pattern: "KOREA_EVENT_SET_POLICY"
    - from: "src/analysis/korea_event_study.py"
      to: "config.FOLLOW_ON_STUDY_END"
      via: "Korea estimation uses the 2026-04-30 follow-on horizon"
      pattern: "FOLLOW_ON_STUDY_END"
    - from: "tests/test_phase7.py"
      to: "output/tables/korea_event_study_car.csv"
      via: "tests assert cohort count and event window coverage"
      pattern: "korea_event_study_car\\.csv"
---

<objective>
Implement the Korea event-study outputs.

Purpose: run a Korea-side staged event study using the primary narrow 2024 Value-Up event set, write machine-readable CAR output plus paper-ready table and figure artifacts, and make the overlap/window choices explicit.

Output: updated `src/analysis/korea_event_study.py`, any needed shared-core adjustments, updated `tests/test_phase7.py`, and Korea artifacts in `output/`.
</objective>

<execution_context>
@/Users/dandan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dandan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/REQUIREMENTS.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/07-korea-value-up-event-study/07-RESEARCH.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/07-korea-value-up-event-study/07-VALIDATION.md
@/Users/dandan/Desktop/Projects/kor-discount/config.py
@/Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study_core.py
@/Users/dandan/Desktop/Projects/kor-discount/src/analysis/korea_event_study.py
@/Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study.py
@/Users/dandan/Desktop/Projects/kor-discount/tests/test_phase7.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the Korea primary policy into the estimator and write Korea CAR artifacts</name>
  <files>src/analysis/event_study_core.py, src/analysis/korea_event_study.py, output/tables/korea_event_study_car.csv, output/tables/table_korea_event_study_coefs.tex</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study_core.py (shared estimator/output logic being finalized)
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/korea_event_study.py (Korea scaffold from 07-01)
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study.py (Japan reference style and output contract)
    - /Users/dandan/Desktop/Projects/kor-discount/config.py (primary Korea policy and follow-on study end)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/07-korea-value-up-event-study/07-RESEARCH.md (artifact naming and policy decisions)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase7.py (target Korea contract)
  </read_first>
  <action>
Finalize the Korea script so `python src/analysis/korea_event_study.py` performs a full local run.

Implementation requirements:

1. Load the canonical panel from `config.PROCESSED_DIR / "panel.parquet"`.
2. Resolve:

```python
primary_policy = config.KOREA_EVENT_SET_POLICY["primary"]
event_dates = primary_policy["dates"]
event_labels = primary_policy["labels"]
max_post_months = int(primary_policy["max_post_months"])
study_end = config.FOLLOW_ON_STUDY_END
```

3. Use the shared core with these concrete Korea windows:

```python
stack_window_min = -36
stack_window_max = max_post_months
event_window_min = -12
event_window_max = max_post_months
```

4. Write these exact outputs:

```python
output/tables/korea_event_study_car.csv
output/tables/table_korea_event_study_coefs.tex
```

5. Ensure the machine-readable CSV uses the same descriptive CAR columns as the current Japan output:

```text
cohort,event_label,event_rel_time,coefficient,car
```

6. Ensure the Korea LaTeX table uses the same descriptive framing as the Japan table and includes an explicit note or comment mentioning overlap or the shortened Korea post window so the clustering choice is not silent.

Do not rewrite the Japan CSV or Japan table in this task. The Korea run must be isolated to Korea artifact paths.
  </action>
  <acceptance_criteria>
    - `grep -q "FOLLOW_ON_STUDY_END" src/analysis/korea_event_study.py` exits 0.
    - `grep -q "max_post_months" src/analysis/korea_event_study.py` exits 0.
    - `python src/analysis/korea_event_study.py` exits 0.
    - `test -s output/tables/korea_event_study_car.csv` exits 0.
    - `test -s output/tables/table_korea_event_study_coefs.tex` exits 0.
    - `python -c "import pandas as pd; df = pd.read_csv('output/tables/korea_event_study_car.csv'); assert list(df.columns) == ['cohort','event_label','event_rel_time','coefficient','car']; print(df['cohort'].nunique())"` prints `3`.
    - `grep -qi "overlap\\|shortened Korea post window\\|max_post_months" output/tables/table_korea_event_study_coefs.tex` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && python src/analysis/korea_event_study.py</automated>
  </verify>
  <done>The Korea CAR CSV and descriptive LaTeX table exist, use the primary policy, and make the overlap/window choice explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Render the Korea figure and finalize the Phase 7 test gate</name>
  <files>src/analysis/event_study_core.py, src/analysis/korea_event_study.py, tests/test_phase7.py, output/figures/figure_korea_event_study.pdf</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/korea_event_study.py (file being finalized)
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study.py (Japan figure style reference)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase7.py (tests that must now pass)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/07-korea-value-up-event-study/07-RESEARCH.md (same-style-but-distinct output requirement)
  </read_first>
  <action>
Extend the Korea run so it writes `output/figures/figure_korea_event_study.pdf` in the same visual style family as the Japan event-study figure:

- `matplotlib.use("Agg")` before pyplot import
- `seaborn.set_theme(style="whitegrid")`
- one subplot per Korea event date from the primary policy
- x-axis label `Months relative to reform`
- y-axis label `CAR: KOSPI - TOPIX P/B`
- deterministic PDF metadata with `CreationDate` and `ModDate` set to `None`

Use a Korea-specific title string and Korea-specific output path. Do not reuse `figure2_event_study.pdf`.

Update `tests/test_phase7.py` so the full file now passes after Korea outputs exist. In particular:

- `test_phase7_korea_outputs_exist` must check the three Korea artifact paths exist and are non-empty,
- `test_phase7_korea_output_window_matches_policy` must use `config.KOREA_EVENT_SET_POLICY["primary"]["max_post_months"]`,
- `test_phase7_japan_artifacts_still_exist` must still validate the Japan figure/table paths.
  </action>
  <acceptance_criteria>
    - `grep -q "figure_korea_event_study.pdf" src/analysis/korea_event_study.py` exits 0.
    - `python src/analysis/korea_event_study.py` exits 0.
    - `test -s output/figures/figure_korea_event_study.pdf` exits 0.
    - `pytest tests/test_phase7.py -q` exits 0.
    - `python -c "import pandas as pd, config; df = pd.read_csv('output/tables/korea_event_study_car.csv'); expected = set(range(-12, config.KOREA_EVENT_SET_POLICY['primary']['max_post_months'] + 1)); assert all(set(g['event_rel_time']) == expected for _, g in df.groupby('cohort')); print('phase7-window-ok')"` prints `phase7-window-ok`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && python src/analysis/korea_event_study.py && pytest tests/test_phase7.py -q</automated>
  </verify>
  <done>The Korea figure exists, the Phase 7 test gate passes, and the Korea output contract matches the primary policy window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical panel -> Korea estimator | The Korea script must read only the checked-in canonical panel and fixed config constants. |
| Korea estimator -> output paths | Korea artifacts must be written to Korea-specific fixed filenames only. |
| shortened window policy -> paper-facing artifacts | The phase must disclose that the Korea post window is policy-limited rather than silently reusing Japan's +24 months. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | global event defaults | mitigate | Drive Korea solely from `config.KOREA_EVENT_SET_POLICY["primary"]` and separate Korea filenames. |
| T-07-02-02 | Repudiation | clustered-date handling | mitigate | Require an explicit overlap or shortened-window note in the Korea output path or generator source. |
| T-07-02-03 | Denial of Service | artifact overwrite | mitigate | Keep Korea writes isolated to `figure_korea_event_study.pdf`, `korea_event_study_car.csv`, and `table_korea_event_study_coefs.tex`. |
| T-07-02-04 | High severity appsec | local analysis scripts | accept | This task runs only local Python and file writes against fixed local paths. |
</threat_model>

<verification>
- `python src/analysis/korea_event_study.py` exits 0.
- `test -s output/figures/figure_korea_event_study.pdf` exits 0.
- `test -s output/tables/korea_event_study_car.csv` exits 0.
- `test -s output/tables/table_korea_event_study_coefs.tex` exits 0.
- `pytest tests/test_phase7.py -q` exits 0.
</verification>

<success_criteria>
- The Korea script produces all three Korea artifacts.
- The Korea output window matches the Phase 6 primary policy.
- Korea overlap/window handling is explicit rather than silent.
- The Phase 7 pytest gate passes.
- Japan output paths are untouched by the Korea run.
</success_criteria>

<output>
After completion, create `.planning/phases/07-korea-value-up-event-study/07-02-SUMMARY.md`.
</output>
