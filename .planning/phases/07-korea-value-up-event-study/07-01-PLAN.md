---
phase: 07-korea-value-up-event-study
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase7.py
  - src/analysis/event_study_core.py
  - src/analysis/event_study.py
  - src/analysis/korea_event_study.py
autonomous: true
requirements:
  - KEVNT-03
  - KEVNT-04

must_haves:
  truths:
    - "Japan event-study execution remains available from `src/analysis/event_study.py` with its current artifact paths"
    - "Korea event-study execution has its own standalone entrypoint and does not depend on mutating `config.EVENT_DATES` globally"
    - "Shared cohort-building and output-writing helpers are factored into a reusable event-study core"
    - "A dedicated `tests/test_phase7.py` contract exists for Korea outputs and Japan non-regression"
  artifacts:
    - path: "tests/test_phase7.py"
      provides: "Phase 7 Korea event-study contract coverage"
    - path: "src/analysis/event_study_core.py"
      provides: "Shared event-study helpers reused by Japan and Korea entrypoints"
    - path: "src/analysis/event_study.py"
      provides: "Preserved Japan entrypoint wired through the shared core"
    - path: "src/analysis/korea_event_study.py"
      provides: "Standalone Korea event-study entrypoint using the Phase 6 primary policy"
  key_links:
    - from: "tests/test_phase7.py"
      to: "src/analysis/korea_event_study.py"
      via: "tests assert the Korea script and artifact paths exist"
      pattern: "korea_event_study"
    - from: "tests/test_phase7.py"
      to: "config.py"
      via: "tests assert the primary Korea policy is used rather than global event reassignment"
      pattern: "KOREA_EVENT_SET_POLICY"
    - from: "src/analysis/event_study.py"
      to: "src/analysis/event_study_core.py"
      via: "Japan entrypoint delegates reusable work to the shared core"
      pattern: "event_study_core"
---

<objective>
Create the reusable Phase 7 foundation.

Purpose: factor the shipped Japan event-study logic into a reusable shared core, add a Korea-specific standalone entrypoint, and create a dedicated Phase 7 test contract that guards against silent Japan regression or Korea artifact-path collisions.

Output: `tests/test_phase7.py`, `src/analysis/event_study_core.py`, updated `src/analysis/event_study.py`, and new `src/analysis/korea_event_study.py`.
</objective>

<execution_context>
@/Users/dandan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dandan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/REQUIREMENTS.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/07-korea-value-up-event-study/07-RESEARCH.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/07-korea-value-up-event-study/07-VALIDATION.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-VERIFICATION.md
@/Users/dandan/Desktop/Projects/kor-discount/config.py
@/Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study.py
@/Users/dandan/Desktop/Projects/kor-discount/src/analysis/study_window.py
@/Users/dandan/Desktop/Projects/kor-discount/tests/test_phase3.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 7 Korea event-study test contract</name>
  <files>tests/test_phase7.py</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/07-korea-value-up-event-study/07-VALIDATION.md (required quick/full commands and contract scope)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/07-korea-value-up-event-study/07-RESEARCH.md (required architecture and artifact-path decisions)
    - /Users/dandan/Desktop/Projects/kor-discount/config.py (primary policy and artifact roots)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase3.py (bootstrap and assertion style to mirror)
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study.py (current Japan entrypoint surface to preserve)
  </read_first>
  <action>
Create `tests/test_phase7.py` using the same bootstrap pattern as the existing repo tests.

Define these exact path constants:

```python
OUTPUT_FIGURES = config.OUTPUT_DIR / "figures"
OUTPUT_TABLES = config.OUTPUT_DIR / "tables"
JAPAN_FIGURE = OUTPUT_FIGURES / "figure2_event_study.pdf"
JAPAN_TABLE = OUTPUT_TABLES / "table_event_study_coefs.tex"
KOREA_FIGURE = OUTPUT_FIGURES / "figure_korea_event_study.pdf"
KOREA_CAR = OUTPUT_TABLES / "korea_event_study_car.csv"
KOREA_TABLE = OUTPUT_TABLES / "table_korea_event_study_coefs.tex"
```

The file must define these test functions:

```python
def test_phase7_primary_policy_contract(): ...
def test_phase7_korea_script_exists(): ...
def test_phase7_output_paths_are_distinct(): ...
def test_phase7_korea_outputs_exist(): ...
def test_phase7_korea_output_window_matches_policy(): ...
def test_phase7_korea_source_mentions_overlap_handling(): ...
def test_phase7_japan_artifacts_still_exist(): ...
```

Test expectations:

- `test_phase7_primary_policy_contract` asserts `config.KOREA_EVENT_SET_POLICY["primary"]` exists, points to `config.KOREA_VALUE_UP_NARROW_EVENT_DATES`, points to `config.KOREA_VALUE_UP_NARROW_EVENT_LABELS`, and exposes an integer `max_post_months`.
- `test_phase7_korea_script_exists` asserts `src/analysis/korea_event_study.py` exists.
- `test_phase7_output_paths_are_distinct` asserts the Korea output filenames above are not equal to the shipped Japan filenames.
- `test_phase7_korea_outputs_exist` checks the Korea CSV, LaTeX, and PDF artifact paths exist and are non-empty. This test is expected to fail until Plan 07-02 executes.
- `test_phase7_korea_output_window_matches_policy` reads `korea_event_study_car.csv`, asserts there are three cohorts, and asserts every cohort covers exactly `range(-12, max_post_months + 1)` using `config.KOREA_EVENT_SET_POLICY["primary"]["max_post_months"]`.
- `test_phase7_korea_source_mentions_overlap_handling` reads `src/analysis/korea_event_study.py` and/or `src/analysis/event_study_core.py` and asserts the source contains `overlap` plus either `KOREA_EVENT_SET_POLICY` or `max_post_months`.
- `test_phase7_japan_artifacts_still_exist` checks `figure2_event_study.pdf` and `table_event_study_coefs.tex` still exist and are non-empty.

The Task 1 gate is collection only. Do not make the new tests pass yet; make them collect cleanly.
  </action>
  <acceptance_criteria>
    - `grep -q "def test_phase7_primary_policy_contract" tests/test_phase7.py` exits 0.
    - `grep -q "def test_phase7_korea_script_exists" tests/test_phase7.py` exits 0.
    - `grep -q "def test_phase7_output_paths_are_distinct" tests/test_phase7.py` exits 0.
    - `grep -q "def test_phase7_korea_outputs_exist" tests/test_phase7.py` exits 0.
    - `grep -q "def test_phase7_korea_output_window_matches_policy" tests/test_phase7.py` exits 0.
    - `grep -q "def test_phase7_korea_source_mentions_overlap_handling" tests/test_phase7.py` exits 0.
    - `grep -q "def test_phase7_japan_artifacts_still_exist" tests/test_phase7.py` exits 0.
    - `pytest tests/test_phase7.py --collect-only -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && pytest tests/test_phase7.py --collect-only -q</automated>
  </verify>
  <done>`tests/test_phase7.py` exists, collects successfully, and encodes the Korea-versus-Japan output contract.</done>
</task>

<task type="auto">
  <name>Task 2: Factor a shared event-study core and add the Korea entrypoint scaffold</name>
  <files>src/analysis/event_study_core.py, src/analysis/event_study.py, src/analysis/korea_event_study.py</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study.py (file being refactored; preserve current Japan CLI behavior)
    - /Users/dandan/Desktop/Projects/kor-discount/config.py (Japan defaults and Korea primary policy)
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/study_window.py (shared horizon helper already introduced in Phase 6)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/07-korea-value-up-event-study/07-RESEARCH.md (target architecture and filename decisions)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase3.py (Japan behavior that must keep working)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase7.py (new target behavior)
  </read_first>
  <action>
Create `src/analysis/event_study_core.py` and move the reusable event-study logic there rather than duplicating the current Japan estimator.

The shared module must expose a concrete reusable API, either as functions or a small dataclass plus functions, that can accept these exact inputs:

- `event_dates`
- `event_labels`
- `study_end`
- `stack_window_min`
- `stack_window_max`
- `event_window_min`
- `event_window_max`
- `figure_title`
- `figure_output_path`
- `car_output_path`
- `table_output_path`

Then update `src/analysis/event_study.py` so:

- it still remains the Japan standalone entrypoint,
- it still writes exactly `output/figures/figure2_event_study.pdf`,
- it still writes exactly `output/tables/event_study_car.csv`,
- it still writes exactly `output/tables/table_event_study_coefs.tex`,
- it reads Japan dates and labels from `config.JAPAN_EVENT_DATES` and `config.JAPAN_EVENT_LABELS` or the preserved Japan aliases,
- it delegates cohort construction/output writing to `event_study_core.py`.

Create `src/analysis/korea_event_study.py` as a standalone executable script using the same project-root bootstrap pattern as the existing analysis scripts.

In `src/analysis/korea_event_study.py`, scaffold these exact output paths:

```python
config.OUTPUT_DIR / "figures" / "figure_korea_event_study.pdf"
config.OUTPUT_DIR / "tables" / "korea_event_study_car.csv"
config.OUTPUT_DIR / "tables" / "table_korea_event_study_coefs.tex"
```

Use the Phase 6 primary policy explicitly:

```python
primary_policy = config.KOREA_EVENT_SET_POLICY["primary"]
```

and pass:

- `primary_policy["dates"]`
- `primary_policy["labels"]`
- `config.FOLLOW_ON_STUDY_END`
- `primary_policy["max_post_months"]`

to the shared core rather than mutating `config.EVENT_DATES` or `config.EVENT_LABELS`.

For the scaffold stage, keep these exact constants visible in the Korea script:

```python
KOREA_STACK_WINDOW_MIN = -36
KOREA_EVENT_WINDOW_MIN = -12
```

and make `KOREA_EVENT_WINDOW_MAX` come from `primary_policy["max_post_months"]`.

Preserve explicit overlap handling in the shared code. The source must still expose overlap-related logic and comments rather than silently dropping clustered months.
  </action>
  <acceptance_criteria>
    - `test -f src/analysis/event_study_core.py` exits 0.
    - `grep -q "event_dates" src/analysis/event_study_core.py` exits 0.
    - `grep -q "figure_output_path" src/analysis/event_study_core.py` exits 0.
    - `grep -q "event_study_core" src/analysis/event_study.py` exits 0.
    - `grep -q "figure2_event_study.pdf" src/analysis/event_study.py` exits 0.
    - `test -f src/analysis/korea_event_study.py` exits 0.
    - `grep -q "primary_policy = config.KOREA_EVENT_SET_POLICY\\[\"primary\"\\]" src/analysis/korea_event_study.py` exits 0.
    - `grep -q "figure_korea_event_study.pdf" src/analysis/korea_event_study.py` exits 0.
    - `grep -q "korea_event_study_car.csv" src/analysis/korea_event_study.py` exits 0.
    - `grep -q "table_korea_event_study_coefs.tex" src/analysis/korea_event_study.py` exits 0.
    - `grep -q "KOREA_EVENT_WINDOW_MIN = -12" src/analysis/korea_event_study.py` exits 0.
    - `grep -q "overlap" src/analysis/event_study_core.py` exits 0.
    - `python src/analysis/event_study.py` exits 0.
    - `pytest tests/test_phase3.py::test_three_cohorts tests/test_phase3.py::test_figure2_panels tests/test_phase3.py::test_event_study_coefs -q` exits 0.
    - `pytest tests/test_phase7.py --collect-only -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && python src/analysis/event_study.py && pytest tests/test_phase3.py::test_three_cohorts tests/test_phase3.py::test_figure2_panels tests/test_phase3.py::test_event_study_coefs -q && pytest tests/test_phase7.py --collect-only -q</automated>
  </verify>
  <done>The repo has a reusable event-study core, a preserved Japan entrypoint that still passes targeted Phase 3 checks, and a Korea entrypoint scaffold with distinct artifact paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared core -> Japan entrypoint | Refactoring must preserve the shipped Japan CLI and artifact paths. |
| shared core -> Korea entrypoint | Korea logic must use explicit policy inputs rather than global event-date mutation. |
| tests -> artifact names | Phase 7 tests enforce path separation so Korea outputs cannot silently overwrite Japan outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | Japan event-study defaults | mitigate | Preserve `src/analysis/event_study.py` as the Japan entrypoint and keep its existing output filenames explicit in source and tests. |
| T-07-01-02 | Repudiation | Korea event-set selection | mitigate | Require the Korea script to read `config.KOREA_EVENT_SET_POLICY["primary"]` directly. |
| T-07-01-03 | Denial of Service | overlapping Korea windows | mitigate | Preserve overlap annotations in shared logic and test for source-level overlap handling. |
| T-07-01-04 | High severity appsec | local analysis scripts | accept | This phase touches only local Python scripts, local files, and fixed output paths; there is no network, auth, or secret boundary. |
</threat_model>

<verification>
- `python src/analysis/event_study.py` exits 0.
- `pytest tests/test_phase3.py::test_three_cohorts tests/test_phase3.py::test_figure2_panels tests/test_phase3.py::test_event_study_coefs -q` exits 0.
- `pytest tests/test_phase7.py --collect-only -q` exits 0.
- `grep -q "primary_policy = config.KOREA_EVENT_SET_POLICY\\[\"primary\"\\]" src/analysis/korea_event_study.py` exits 0.
- `grep -q "figure2_event_study.pdf" src/analysis/event_study.py` exits 0.
- `grep -q "figure_korea_event_study.pdf" src/analysis/korea_event_study.py` exits 0.
</verification>

<success_criteria>
- `tests/test_phase7.py` exists and collects.
- The repo has a shared event-study core.
- Japan keeps its current entrypoint and output names.
- Korea has its own entrypoint and distinct output names.
- The design makes explicit use of the Phase 6 primary Korea policy.
</success_criteria>

<output>
After completion, create `.planning/phases/07-korea-value-up-event-study/07-01-SUMMARY.md`.
</output>
