---
phase: 06-korea-reform-date-locking-and-sample-horizon
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - config.py
  - src/analysis/study_window.py
  - src/analysis/event_study.py
  - tests/test_phase6.py
autonomous: true
requirements:
  - DATES-02
  - SAMPLE-01

must_haves:
  truths:
    - "Japan event constants remain backwards compatible for shipped v1.0 scripts"
    - "Korea reform date sets are stored separately from Japan event collections"
    - "The codebase exposes an explicit extended study end of 2026-04-30 without forcing all existing outputs to adopt it"
    - "Phase 7 has a machine-readable policy telling it which Korea date set is primary, which is robustness, and what post-window limits the 2026-04-30 endpoint allows"
  artifacts:
    - path: "config.py"
      provides: "Separate Japan and Korea event-date surfaces plus study-end constants and policy"
      contains: "JAPAN_EVENT_DATES, KOREA_VALUE_UP_NARROW_EVENT_DATES, KOREA_VALUE_UP_SPACED_EVENT_DATES, PAPER_STUDY_END, FOLLOW_ON_STUDY_END, KOREA_EVENT_SET_POLICY"
    - path: "src/analysis/study_window.py"
      provides: "Shared study-window helpers for later Korea follow-on code"
    - path: "src/analysis/event_study.py"
      provides: "Production event-study path with opt-in study-end preparation logic"
    - path: "tests/test_phase6.py"
      provides: "Regression coverage for config separation and follow-on horizon"
  key_links:
    - from: "tests/test_phase6.py"
      to: "config.py"
      via: "tests assert date-set separation, alias preservation, and machine-readable policy"
      pattern: "KOREA_EVENT_SET_POLICY"
    - from: "tests/test_phase6.py"
      to: "src/analysis/study_window.py"
      via: "tests assert the follow-on horizon clips data through 2026-04-30"
      pattern: "clip_to_study_end"
    - from: "tests/test_phase6.py"
      to: "src/analysis/event_study.py"
      via: "tests assert the real event-study preparation path accepts the opt-in follow-on horizon"
      pattern: "prepare_event_study_panel"
---

<objective>
Add the configuration and helper plumbing that Phase 7 will need.

Purpose: expose separate Korea reform-date collections, a machine-readable primary-vs-robustness policy, and a real event-study production path that can consume the follow-on study horizon through `2026-04-30` while preserving the current Japan defaults used by the shipped paper.

Output: updated `config.py`, new `src/analysis/study_window.py`, updated `src/analysis/event_study.py`, and `tests/test_phase6.py`.
</objective>

<execution_context>
@/Users/dandan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dandan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/REQUIREMENTS.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-RESEARCH.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-VALIDATION.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-DATE-LOCK.md
@/Users/dandan/Desktop/Projects/kor-discount/config.py
@/Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study.py
@/Users/dandan/Desktop/Projects/kor-discount/src/robustness/robustness_pe.py
@/Users/dandan/Desktop/Projects/kor-discount/src/robustness/robustness_placebo.py
@/Users/dandan/Desktop/Projects/kor-discount/src/data/verify_panel.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 6 regression tests for date-set separation and horizon plumbing</name>
  <files>tests/test_phase6.py</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-VALIDATION.md (required test surface and quick command)
    - /Users/dandan/Desktop/Projects/kor-discount/config.py (existing event-date API that must remain compatible)
    - /Users/dandan/Desktop/Projects/kor-discount/src/data/verify_panel.py (panel date expectations already present)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase3.py (pytest bootstrap and repo import pattern)
  </read_first>
  <action>
Create `tests/test_phase6.py` using the same bootstrap pattern as existing repo tests.

The file must define these exact test functions:

```python
def test_japan_event_aliases_preserved(): ...
def test_korea_value_up_date_sets_locked(): ...
def test_korea_event_policy_contract(): ...
def test_study_end_constants_include_follow_on_window(): ...
def test_clip_to_study_end_allows_2026_04_30_rows(): ...
def test_prepare_event_study_panel_accepts_follow_on_window(): ...
```

Test expectations:

- `test_japan_event_aliases_preserved` must assert that `config.EVENT_DATES == config.JAPAN_EVENT_DATES` and `config.EVENT_LABELS == config.JAPAN_EVENT_LABELS`.
- `test_korea_value_up_date_sets_locked` must assert that:
  - `config.KOREA_VALUE_UP_NARROW_EVENT_DATES` contains exactly `2024-02-26`, `2024-05-02`, `2024-08-12`
  - `config.KOREA_VALUE_UP_SPACED_EVENT_DATES` contains exactly `2024-02-26`, `2025-07-09`, `2026-02-24`
- `test_korea_event_policy_contract` must assert that `config.KOREA_EVENT_SET_POLICY` exists and contains:
  - a `primary` entry pointing to the narrow 2024 rollout set
  - a `robustness` entry pointing to the spaced follow-through set
  - explicit integer `max_post_months` values for both entries
- `test_study_end_constants_include_follow_on_window` must assert that `config.PAPER_STUDY_END` equals `datetime.date(2024, 12, 31)` and `config.FOLLOW_ON_STUDY_END` equals `datetime.date(2026, 4, 30)`.
- `test_clip_to_study_end_allows_2026_04_30_rows` must read `data/processed/panel.parquet`, import `clip_to_study_end` from `src.analysis.study_window`, filter with `config.FOLLOW_ON_STUDY_END`, and assert the resulting max date is `Timestamp("2026-04-30")`.
- `test_prepare_event_study_panel_accepts_follow_on_window` must import `prepare_event_study_panel` from `src.analysis.event_study`, pass it the canonical panel and `config.FOLLOW_ON_STUDY_END`, and assert the prepared frame reaches `Timestamp("2026-04-30")`.

The file must collect successfully before implementation is complete.
  </action>
  <acceptance_criteria>
    - `grep -q "def test_japan_event_aliases_preserved" tests/test_phase6.py` exits 0.
    - `grep -q "def test_korea_value_up_date_sets_locked" tests/test_phase6.py` exits 0.
    - `grep -q "def test_korea_event_policy_contract" tests/test_phase6.py` exits 0.
    - `grep -q "def test_study_end_constants_include_follow_on_window" tests/test_phase6.py` exits 0.
    - `grep -q "def test_clip_to_study_end_allows_2026_04_30_rows" tests/test_phase6.py` exits 0.
    - `grep -q "def test_prepare_event_study_panel_accepts_follow_on_window" tests/test_phase6.py` exits 0.
    - `pytest tests/test_phase6.py --collect-only -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && pytest tests/test_phase6.py --collect-only -q</automated>
  </verify>
  <done>`tests/test_phase6.py` exists and collects the Phase 6 regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add separate Korea date sets, machine-readable policy, and event-study horizon plumbing</name>
  <files>config.py, src/analysis/study_window.py, src/analysis/event_study.py</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/config.py (file being modified; preserve existing Japan constants)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-DATE-LOCK.md (exact dates and primary-vs-robustness decision)
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/event_study.py (current Japan consumers of EVENT_DATES and EVENT_LABELS)
    - /Users/dandan/Desktop/Projects/kor-discount/src/robustness/robustness_pe.py (same Japan-event consumer pattern)
    - /Users/dandan/Desktop/Projects/kor-discount/src/robustness/robustness_placebo.py (same Japan-event consumer pattern)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase6.py (target behavior to satisfy)
  </read_first>
  <action>
Update `config.py` to add these exact public constants while preserving all existing Japan names:

```python
JAPAN_EVENT_DATES
JAPAN_EVENT_LABELS
KOREA_VALUE_UP_NARROW_EVENT_DATES
KOREA_VALUE_UP_NARROW_EVENT_LABELS
KOREA_VALUE_UP_SPACED_EVENT_DATES
KOREA_VALUE_UP_SPACED_EVENT_LABELS
PAPER_STUDY_END
FOLLOW_ON_STUDY_END
KOREA_EVENT_SET_POLICY
```

Implementation rules:

- Keep the existing Japan single-date constants (`STEWARDSHIP_CODE_DATE`, `CGC_DATE`, `TSE_PB_REFORM_DATE`) intact.
- Set `JAPAN_EVENT_DATES` and `JAPAN_EVENT_LABELS` to the current Japan collections.
- Keep backwards-compatible aliases by assigning:

```python
EVENT_DATES = JAPAN_EVENT_DATES
EVENT_LABELS = JAPAN_EVENT_LABELS
```

- Set:

```python
PAPER_STUDY_END = datetime.date(2024, 12, 31)
FOLLOW_ON_STUDY_END = datetime.date(2026, 4, 30)
```

Add `KOREA_EVENT_SET_POLICY` as a machine-readable dict with exactly two top-level keys:

```python
"primary"
"robustness"
```

Each entry must contain:

```python
"set_name"
"dates"
"labels"
"max_post_months"
```

Set the primary entry to the narrow 2024 rollout set and the robustness entry to the spaced follow-through set. Use integer `max_post_months` values derived from the `2026-04-30` endpoint.

Create `src/analysis/study_window.py` with:

```python
def clip_to_study_end(frame: pd.DataFrame, study_end: datetime.date | pd.Timestamp) -> pd.DataFrame
```

Behavior:

- Convert `study_end` to `pd.Timestamp`
- Return `frame.loc[frame["date"] <= resolved_end].copy()`
- Raise `ValueError("frame must contain a 'date' column")` if `date` is missing

Update `src/analysis/event_study.py` to add a production-path preparation function:

```python
def prepare_event_study_panel(
    panel: pd.DataFrame,
    study_end: datetime.date | pd.Timestamp,
    metric: str = "pb",
) -> pd.DataFrame
```

Behavior:

- Require `date`, `country`, and the chosen metric column
- Convert `date` to `datetime64`
- Use `clip_to_study_end` from `src.analysis.study_window`
- Return a copy restricted to the requested columns

Then update `build_stacked_dataset` so it calls `prepare_event_study_panel(panel, config.PAPER_STUDY_END, metric="pb")` before building cohorts. This keeps current Japan behavior intact while wiring the real event-study module to the centralized horizon path.

Do not change `src/robustness/robustness_pe.py` or `src/robustness/robustness_placebo.py` in this task unless a small import/type fix is required by the new tests. The shipped Japan default behavior must stay intact.
  </action>
  <acceptance_criteria>
    - `grep -q "JAPAN_EVENT_DATES" config.py` exits 0.
    - `grep -q "KOREA_VALUE_UP_NARROW_EVENT_DATES" config.py` exits 0.
    - `grep -q "KOREA_VALUE_UP_SPACED_EVENT_DATES" config.py` exits 0.
    - `grep -q "PAPER_STUDY_END" config.py` exits 0.
    - `grep -q "FOLLOW_ON_STUDY_END" config.py` exits 0.
    - `grep -q "KOREA_EVENT_SET_POLICY" config.py` exits 0.
    - `grep -q "EVENT_DATES = JAPAN_EVENT_DATES" config.py` exits 0.
    - `grep -q "EVENT_LABELS = JAPAN_EVENT_LABELS" config.py` exits 0.
    - `test -f src/analysis/study_window.py` exits 0.
    - `grep -q "def clip_to_study_end" src/analysis/study_window.py` exits 0.
    - `grep -q "def prepare_event_study_panel" src/analysis/event_study.py` exits 0.
    - `pytest tests/test_phase6.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && pytest tests/test_phase6.py -q</automated>
  </verify>
  <done>The repo exposes separate Japan/Korea event collections, an explicit policy for primary versus robustness date sets, and a real event-study code path wired to the centralized horizon helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config constants -> shipped scripts | Existing Japan analysis imports generic event aliases from `config.py` and must not change behavior. |
| helper module -> future Korea analyses | Later phases will trust `clip_to_study_end` and follow-on constants for the 2026 path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | shipped Japan defaults | mitigate | Preserve `EVENT_DATES` and `EVENT_LABELS` as aliases to Japan collections and test that aliasing explicitly. |
| T-06-02-02 | Denial of Service | future Korea phase blocked by missing shared horizon control | mitigate | Add `FOLLOW_ON_STUDY_END` and `clip_to_study_end` with regression tests. |
| T-06-02-03 | Repudiation | ambiguous event-set ownership | mitigate | Add `KOREA_EVENT_SET_POLICY` so Phase 7 consumes explicit primary/robustness policy instead of prose only. |
| T-06-02-04 | Tampering | event-study preparation path | mitigate | Wire `prepare_event_study_panel` into `build_stacked_dataset` so the real module uses centralized horizon logic instead of scattered literals. |
| T-06-02-05 | High severity appsec | local config/helpers | accept | No auth, network, or secret boundary is introduced; risk is correctness, not appsec. |
</threat_model>

<verification>
- `pytest tests/test_phase6.py -q` exits 0.
- `python -c "import config; assert config.EVENT_DATES == config.JAPAN_EVENT_DATES; assert config.KOREA_EVENT_SET_POLICY['primary']['set_name']; assert config.FOLLOW_ON_STUDY_END.isoformat() == '2026-04-30'; print('phase6-config-ok')"` prints `phase6-config-ok`.
</verification>

<success_criteria>
- `config.py` exposes separate Japan and Korea date sets.
- `config.py` exposes a machine-readable Korea event policy for primary and robustness use.
- Backwards-compatible Japan aliases are preserved.
- The repo exposes an explicit follow-on study end of `2026-04-30` and the real event-study module consumes the centralized horizon-prep path.
- `tests/test_phase6.py` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-02-SUMMARY.md`.
</output>
