---
phase: 06-korea-reform-date-locking-and-sample-horizon
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified: []
autonomous: true
requirements:
  - SAMPLE-01
  - SAMPLE-02

must_haves:
  truths:
    - "The panel reaches 2026-04-30"
    - "Phase 6 config changes do not break existing repo tests"
    - "The phase-local memo, config surface, and helper module are all present and internally consistent"
  artifacts:
    - path: ".planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-DATE-LOCK.md"
      provides: "Operational memo for Phase 7 hand-off"
    - path: "tests/test_phase6.py"
      provides: "Phase 6 regression coverage"
    - path: "src/analysis/study_window.py"
      provides: "Shared follow-on horizon helper"
  key_links:
    - from: "tests/test_phase6.py"
      to: "data/processed/panel.parquet"
      via: "follow-on horizon test must observe 2026-04-30 in canonical data"
      pattern: "2026-04-30"
---

<objective>
Run the Phase 6 verification gate.

Purpose: confirm that the date-lock memo exists, the config surface is separated correctly, the canonical panel reaches `2026-04-30`, and the Phase 6 plumbing does not regress the existing project test suite.

Output: no source changes unless a verification failure requires a fix.
</objective>

<execution_context>
@/Users/dandan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dandan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-DATE-LOCK.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-VALIDATION.md
@/Users/dandan/Desktop/Projects/kor-discount/tests/test_phase6.py
@/Users/dandan/Desktop/Projects/kor-discount/config.py
@/Users/dandan/Desktop/Projects/kor-discount/src/analysis/study_window.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify panel coverage and Phase 6-specific regressions</name>
  <files>None - verification-only task</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase6.py (Phase 6 verification contract)
    - /Users/dandan/Desktop/Projects/kor-discount/config.py (study-end and event-set constants)
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/study_window.py (helper contract)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-VALIDATION.md (required quick/full commands)
  </read_first>
  <action>
Run these commands from the project root in this exact order:

```bash
python -c "import pandas as pd; df = pd.read_parquet('data/processed/panel.parquet', columns=['date']); print(df['date'].max()); assert str(df['date'].max().date()) == '2026-04-30'"
pytest tests/test_phase6.py -q
test -s output/figures/figure2_event_study.pdf
test -s output/tables/table_event_study_coefs.tex
pytest -q
```

If any command fails, fix the underlying issue before moving on. Do not skip the full-suite run: the goal of this plan is non-regression, not only local success.
  </action>
  <acceptance_criteria>
    - The panel coverage command prints `2026-04-30 00:00:00` and exits 0.
    - `pytest tests/test_phase6.py -q` exits 0.
    - `test -s output/figures/figure2_event_study.pdf` exits 0.
    - `test -s output/tables/table_event_study_coefs.tex` exits 0.
    - `pytest -q` exits 0.
    - `grep -q "^## Phase 7 Hand-off" .planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-DATE-LOCK.md` exits 0.
    - `python -c "import config; assert config.EVENT_DATES == config.JAPAN_EVENT_DATES; assert config.FOLLOW_ON_STUDY_END.isoformat() == '2026-04-30'; print('phase6-verification-ok')"` prints `phase6-verification-ok`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && python -c "import pandas as pd; df = pd.read_parquet('data/processed/panel.parquet', columns=['date']); print(df['date'].max()); assert str(df['date'].max().date()) == '2026-04-30'" && pytest tests/test_phase6.py -q && test -s output/figures/figure2_event_study.pdf && test -s output/tables/table_event_study_coefs.tex && pytest -q</automated>
  </verify>
  <done>The Phase 6 plumbing is verified, the canonical panel reaches 2026-04-30, and the repo test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical panel -> verification | Verification trusts the checked-in panel to prove the data horizon. |
| config/helper changes -> full suite | Full-suite pytest is used as the non-regression gate for shipped behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | panel horizon assumption | mitigate | Read `panel.parquet` directly and assert the max date is exactly `2026-04-30`. |
| T-06-03-02 | Repudiation | backward compatibility | mitigate | Run the full pytest suite after the local Phase 6 tests. |
| T-06-03-03 | Denial of Service | lingering config regressions | mitigate | Do not complete the plan until both phase-local and full-suite tests pass. |
| T-06-03-04 | High severity appsec | local verification workflow | accept | Verification runs only local Python and pytest commands with no secrets or network boundary. |
</threat_model>

<verification>
- `python -c "import pandas as pd; df = pd.read_parquet('data/processed/panel.parquet', columns=['date']); print(df['date'].max()); assert str(df['date'].max().date()) == '2026-04-30'"` exits 0.
- `pytest tests/test_phase6.py -q` exits 0.
- `test -s output/figures/figure2_event_study.pdf` exits 0.
- `test -s output/tables/table_event_study_coefs.tex` exits 0.
- `pytest -q` exits 0.
</verification>

<success_criteria>
- Canonical data reaches `2026-04-30`.
- Phase 6 tests pass.
- Existing Japan event-study artifact paths remain valid.
- Full repo tests pass after the Phase 6 plumbing changes.
- The Phase 7 hand-off memo is present and ready for the next planning step.
</success_criteria>

<output>
After completion, create `.planning/phases/06-korea-reform-date-locking-and-sample-horizon/06-03-SUMMARY.md`.
</output>
