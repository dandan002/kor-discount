---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements.txt
  - README.md
  - .env.example
  - utils/__init__.py
  - paper/.gitkeep
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-04
  - INFR-05

must_haves:
  truths:
    - "data/raw/prior-project/ exists and holds all 290+ former index CSVs, MANIFEST.md, MISSING.txt, and data_gpr_export.xls"
    - "data/prior-project/ exists and holds panel.parquet from data/processed/"
    - ".gitignore contains data/raw/prior-project/ and data/prior-project/ entries"
    - "utils/ directory exists at project root with __init__.py (Python package per D-03/D-04)"
    - "paper/ directory exists with sections/ and style/ subdirectories"
    - "requirements.txt lists all 8 dependencies with lower-bound version pins"
    - ".env.example exists with BBG_HOST, BBG_PORT, and FSS_API_KEY entries"
    - "README.md explains venv setup and project-root invocation requirement"
  artifacts:
    - path: "utils/__init__.py"
      provides: "Python package marker — enables 'from utils.bbg import bdp'"
    - path: "requirements.txt"
      provides: "All project Python dependencies"
    - path: ".env.example"
      provides: "Bloomberg connection config template"
    - path: "README.md"
      provides: "Setup instructions including venv and run-from-root requirement"
    - path: ".gitignore"
      provides: "Prevents 290 prior-project CSVs and .env from being committed"
  key_links:
    - from: "utils/__init__.py"
      to: "utils/bbg.py, utils/stats.py, utils/latex_tables.py"
      via: "Python package discovery from project root"
      pattern: "from utils\\."
---

<objective>
Stand up the complete project infrastructure: move prior-project data out of the way, create the directory scaffold (utils/, paper/), write requirements.txt, .env.example, and README.md, and update .gitignore so no prior-project CSVs are ever committed.

Purpose: Clears the decks for Phase 1 coding. All subsequent plans assume this structure exists. Per D-03/D-04, utils/ is a proper Python package at project root — this plan creates that package marker.

Output: Clean directory layout matching ROADMAP spec, gitignored prior-project data, and all meta-files that let a new contributor set up and run from scratch.
</objective>

<execution_context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/../.claude/get-shit-done/workflows/execute-plan.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/PROJECT.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/ROADMAP.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/STATE.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key locked decisions governing this plan -->
<!-- D-03: utils/ at project root, not src/utils/ -->
<!-- D-04: utils/ is a proper Python package with __init__.py -->
<!-- D-06: 290 prior-project index CSVs → data/raw/prior-project/; panel.parquet → data/prior-project/ -->
<!-- D-07: data/raw/prior-project/ and data/prior-project/ added to .gitignore, NOT committed -->
<!-- Claude's Discretion: requirements.txt uses >= lower bounds, not exact pins -->

<!-- CRITICAL path note from RESEARCH.md Pitfall 5:
     Update .gitignore BEFORE moving files. If data/raw/prior-project/ is created
     before .gitignore is updated, git may stage the 290 CSVs.
     Order: update .gitignore FIRST, then move files. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore and move prior-project data</name>
  <files>.gitignore, data/raw/prior-project/ (new dir), data/prior-project/ (new dir)</files>

  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/.gitignore (current contents — must extend, not overwrite)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-CONTEXT.md (D-06, D-07)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-RESEARCH.md (Pitfall 5, Runtime State Inventory)
  </read_first>

  <action>
Step 1 — Update .gitignore FIRST (before any files are moved):
Append these lines to .gitignore (do not remove existing entries):

```
# Prior-project data (moved out of active directories — not for this study)
data/raw/prior-project/
data/prior-project/

# Python cache
__pycache__/
*.pyc
```

Step 2 — Create destination directories:
```bash
mkdir -p data/raw/prior-project
mkdir -p data/prior-project
```

Step 3 — Move prior-project files from data/raw/ to data/raw/prior-project/:
Move ALL index CSV files (bovespa_*, sp500_*, dji_*, kospi_*, msci_*, and any other index-level CSVs — approximately 290 files), plus:
- data/raw/MANIFEST.md
- data/raw/MISSING.txt
- data/raw/data_gpr_export.xls

Use: `mv data/raw/*bovespa* data/raw/*dji* data/raw/*sp500* data/raw/*kospi* data/raw/*msci* data/raw/MANIFEST.md data/raw/MISSING.txt data/raw/data_gpr_export.xls data/raw/prior-project/` (adjust glob patterns to match actual filenames; ls data/raw/ first to confirm all non-bloomberg files).

Step 4 — Move panel.parquet:
```bash
mv data/processed/panel.parquet data/prior-project/panel.parquet
```

Step 5 — Remove the empty src/utils/ directory per D-03:
```bash
rmdir src/utils
```
(If src/utils/ has any files, read them first and preserve any useful content before removing.)

Step 6 — Verify git status shows no staged prior-project files (they should be untracked and gitignored):
```bash
git status --short | grep prior-project
```
This should return EMPTY output (no lines). If any prior-project files appear in git status, the gitignore entry was not applied correctly — fix before proceeding.
  </action>

  <verify>
    <automated>
      # .gitignore contains both entries
      grep -c "data/raw/prior-project/" /Users/dandan/Desktop/Projects/kor-discount/.gitignore
      grep -c "data/prior-project/" /Users/dandan/Desktop/Projects/kor-discount/.gitignore
      # prior-project dirs exist
      test -d /Users/dandan/Desktop/Projects/kor-discount/data/raw/prior-project && echo "raw/prior-project OK"
      test -d /Users/dandan/Desktop/Projects/kor-discount/data/prior-project && echo "prior-project OK"
      # MANIFEST moved
      test -f /Users/dandan/Desktop/Projects/kor-discount/data/raw/prior-project/MANIFEST.md && echo "MANIFEST moved"
      # src/utils removed
      test ! -d /Users/dandan/Desktop/Projects/kor-discount/src/utils && echo "src/utils removed"
      # git doesn't track prior-project
      git -C /Users/dandan/Desktop/Projects/kor-discount status --short | grep -c "prior-project" | grep -x "0"
    </automated>
  </verify>

  <done>
    - .gitignore contains entries for data/raw/prior-project/ and data/prior-project/
    - data/raw/prior-project/ exists and contains the 290+ index CSVs, MANIFEST.md, MISSING.txt, and data_gpr_export.xls
    - data/prior-project/ exists and contains panel.parquet
    - src/utils/ directory is removed
    - git status shows no prior-project files as tracked/staged
  </done>
</task>

<task type="auto">
  <name>Task 2: Create directory scaffold, utils package, requirements.txt, .env.example, and README.md</name>
  <files>
    utils/__init__.py,
    paper/.gitkeep,
    paper/sections/.gitkeep,
    paper/style/.gitkeep,
    requirements.txt,
    .env.example,
    README.md
  </files>

  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-RESEARCH.md (Standard Stack section for exact package names and versions, .env.example pattern, Code Examples section)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-CONTEXT.md (D-03, D-04, D-05 for import path rules)
    - /Users/dandan/Desktop/Projects/kor-discount/ROADMAP.md (Phase 0 environment setup, project structure)
  </read_first>

  <action>
1. Create utils/ package at project root (per D-03/D-04):
   Create utils/__init__.py with content:
   ```python
   """
   Korea Discount study utility package.
   
   Usage (from project root):
       from utils.bbg import bdp, bdh, bds
       from utils.stats import winsorize, cohens_kappa
       from utils.latex_tables import df_to_latex
   
   IMPORTANT: Always run scripts from the project root directory:
       python src/00_build_universe.py   # correct
       cd src && python 00_build_universe.py  # WRONG — utils/ won't be found
   """
   ```

2. Create paper/ subdirectory structure with .gitkeep placeholders:
   ```
   paper/.gitkeep
   paper/sections/.gitkeep
   paper/style/.gitkeep
   ```

3. Create requirements.txt with lower-bound pins (Claude's Discretion — no exact pins for terminal compatibility):
   ```
   # Korea Discount — Value-Up Compliance Study
   # Install: pip install -r requirements.txt
   # Note: blpapi installed separately at Bloomberg terminal only
   pandas>=2.2.1
   numpy>=1.26.4
   scipy>=1.12.0
   statsmodels>=0.14.4
   python-dotenv>=1.0.0
   matplotlib>=3.8.3
   seaborn>=0.13.2
   stargazer>=0.0.7
   ```

4. Create .env.example (per INFR-04 and RESEARCH.md Code Examples):
   ```bash
   # Bloomberg terminal connection (default: localhost:8194)
   BBG_HOST=127.0.0.1
   BBG_PORT=8194
   # FSS API key (prior project — retain for reference)
   FSS_API_KEY=
   ```

5. Create README.md (per INFR-05):
   ```markdown
   # Korea Discount: Value-Up Compliance Study
   
   Reproducible empirical pipeline studying KOSPI firms' compliance with Korea's 2024 Corporate Value-Up program.
   
   ## Setup
   
   ```bash
   python -m venv venv
   source venv/bin/activate          # macOS/Linux
   # venv\Scripts\activate           # Windows
   pip install -r requirements.txt
   cp .env.example .env
   # Edit .env if Bloomberg host/port differ from defaults
   ```
   
   ## IMPORTANT: Always run from project root
   
   All scripts must be run from the project root directory so that the `utils/` package is importable:
   
   ```bash
   # CORRECT
   python src/00_build_universe.py
   python src/01_bloomberg_pull.py
   
   # WRONG — will raise ModuleNotFoundError for utils
   cd src && python 00_build_universe.py
   ```
   
   ## Bloomberg terminal session (one-time)
   
   At a Bloomberg terminal:
   ```bash
   pip install blpapi
   python utils/bbg.py --test            # verify connection
   python src/00_build_universe.py       # → data/raw/universe_raw.csv
   python src/01_bloomberg_pull.py       # → data/raw/bloomberg/*.csv
   ls -lh data/raw/bloomberg/            # confirm three non-empty CSVs
   ```
   
   Once `data/raw/bloomberg/` contains snapshot_2023.csv, roe_panel.csv, and returns_panel.csv, all remaining work is offline.
   
   ## Pipeline
   
   ```
   make acquire    # Bloomberg terminal only — pulls raw CSVs
   make analysis   # Offline — builds compliance dataset, runs all analyses
   make paper      # Offline — compiles LaTeX paper to outputs/paper.pdf
   make all        # acquire + analysis + paper
   ```
   
   ## Directory layout
   
   ```
   src/          — analysis scripts (00_build_universe.py through 07_event_study.py)
   utils/        — shared Python package (bbg, stats, latex_tables)
   data/raw/     — Bloomberg CSVs and manual data (never modified)
   data/processed/ — pipeline outputs (sample.csv, compliance.csv, etc.)
   outputs/      — tables (.tex), figures (.pdf), logs
   paper/        — LaTeX paper scaffold
   ```
   ```
  </action>

  <verify>
    <automated>
      test -f /Users/dandan/Desktop/Projects/kor-discount/utils/__init__.py && echo "utils/__init__.py OK"
      test -d /Users/dandan/Desktop/Projects/kor-discount/paper/sections && echo "paper/sections OK"
      test -d /Users/dandan/Desktop/Projects/kor-discount/paper/style && echo "paper/style OK"
      grep -c "pandas>=2.2.1" /Users/dandan/Desktop/Projects/kor-discount/requirements.txt
      grep -c "BBG_HOST" /Users/dandan/Desktop/Projects/kor-discount/.env.example
      grep -c "BBG_PORT" /Users/dandan/Desktop/Projects/kor-discount/.env.example
      grep -c "venv" /Users/dandan/Desktop/Projects/kor-discount/README.md
      grep -c "project root" /Users/dandan/Desktop/Projects/kor-discount/README.md
      # Verify utils is importable from project root (no blpapi needed for __init__ itself)
      cd /Users/dandan/Desktop/Projects/kor-discount && python -c "import utils; print('utils package OK')"
    </automated>
  </verify>

  <done>
    - utils/__init__.py exists at project root with docstring explaining import pattern and run-from-root requirement
    - paper/sections/ and paper/style/ directories exist
    - requirements.txt lists 8 packages with >=version lower-bound pins (no exact pins)
    - .env.example has BBG_HOST, BBG_PORT, and FSS_API_KEY entries
    - README.md documents venv setup, run-from-root requirement, Bloomberg terminal session, and make targets
    - `python -c "import utils"` succeeds from project root
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git | Moving prior-project files; risk of accidentally staging 290 CSVs if .gitignore not updated first |
| .env → scripts | Bloomberg credentials read from environment; .env must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env file | mitigate | .gitignore already contains `.env` entry; .env.example committed instead — never add real credentials to .env.example |
| T-01-02 | Information Disclosure | data/raw/prior-project/ | mitigate | Update .gitignore before moving files; verify with git status that no prior-project files are tracked |
| T-01-03 | Tampering | data/raw/ (raw data immutability) | accept | CLAUDE.md constraint: raw files never modified; pipeline is read-only on data/raw/ |
</threat_model>

<verification>
After both tasks complete:

```bash
# Directory scaffold
cd /Users/dandan/Desktop/Projects/kor-discount
test -d utils && test -f utils/__init__.py && echo "utils package: OK"
test -d paper/sections && test -d paper/style && echo "paper dirs: OK"
test -d data/raw/prior-project && echo "prior-project: OK"
test -d data/prior-project && echo "prior-project panel: OK"

# Key files
test -f requirements.txt && test -f .env.example && test -f README.md && echo "meta-files: OK"

# Gitignore protection
git status --short | grep -c "prior-project" | grep -x "0" && echo "gitignore: OK"

# Import test
python -c "import utils; print('utils importable: OK')"
```
</verification>

<success_criteria>
1. data/raw/ contains ONLY bloomberg/, dart/, krx/, kftc/ directories (all prior-project files moved to data/raw/prior-project/)
2. utils/__init__.py exists — `python -c "import utils"` exits 0 from project root
3. requirements.txt has 8 entries with >= version pins
4. .env.example has BBG_HOST and BBG_PORT entries
5. README.md documents run-from-root requirement explicitly
6. git status shows zero prior-project files as tracked or staged
7. src/utils/ directory no longer exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
