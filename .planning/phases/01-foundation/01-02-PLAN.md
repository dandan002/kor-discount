---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - utils/__init__.py
  - utils/bbg.py
  - utils/stats.py
  - utils/latex_tables.py
autonomous: true
requirements:
  - UTIL-01
  - UTIL-02
  - UTIL-03
  - UTIL-04

must_haves:
  truths:
    - "from utils.bbg import bdp, bdh, bds imports without error (no blpapi installed test)"
    - "from utils.stats import winsorize, cohens_kappa imports without error"
    - "from utils.latex_tables import df_to_latex imports without error"
    - "python utils/bbg.py --test prints informative error and exits non-zero when blpapi absent (UTIL-02 offline check)"
    - "winsorize(arr, lower=0.01, upper=0.01) returns a plain numpy array"
    - "cohens_kappa returns a (kappa, p_value) tuple"
    - "df_to_latex returns a string containing \\begin{table}, \\toprule, \\caption, and \\label"
  artifacts:
    - path: "utils/bbg.py"
      provides: "BDP, BDH, BDS wrappers + graceful ImportError + --test CLI"
      exports: ["bdp", "bdh", "bds"]
    - path: "utils/stats.py"
      provides: "winsorize, cohens_kappa, robust_se helpers"
      exports: ["winsorize", "cohens_kappa", "robust_se"]
    - path: "utils/latex_tables.py"
      provides: "df_to_latex — DataFrame to booktabs .tex fragment"
      exports: ["df_to_latex"]
  key_links:
    - from: "src/00_build_universe.py"
      to: "utils/bbg.py"
      via: "from utils.bbg import bdp, bdh, bds"
      pattern: "from utils\\.bbg import"
    - from: "src/03_merge_covariates.py"
      to: "utils/stats.py"
      via: "from utils.stats import winsorize"
      pattern: "from utils\\.stats import winsorize"
---

<objective>
Implement the three utility modules that all downstream scripts depend on: bbg.py (Bloomberg API wrappers), stats.py (statistical helpers), and latex_tables.py (LaTeX table exporter). All three modules must be importable from the project root without blpapi installed.

Purpose: This is the foundation layer all eight src/ analysis scripts will import from. Getting the contracts exactly right here prevents cascading errors in Phases 2–4.

Output: utils/bbg.py, utils/stats.py, utils/latex_tables.py — all fully implemented and importable.
</objective>

<execution_context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/../.claude/get-shit-done/workflows/execute-plan.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/ROADMAP.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Locked decisions governing this plan -->
<!-- D-03: utils/ at project root -->
<!-- D-04: import as "from utils.bbg import bdp" — no PYTHONPATH manipulation -->
<!-- D-05: scripts run as "python src/00_build_universe.py" from project root -->

<!-- Exact function signatures all downstream scripts will use:
     bdp(securities, fields, overrides=None) -> pd.DataFrame
       securities: str | list[str]   (Bloomberg ticker strings)
       fields: str | list[str]       (Bloomberg field mnemonics)
       overrides: dict | None        (e.g., {"FUNDAMENTAL_DATABASE_DATE": "20231231"})
       returns: DataFrame with index=securities, columns=fields

     bdh(securities, fields, start_date, end_date, periodicity="DAILY") -> pd.DataFrame
       securities: str | list[str]
       fields: str | list[str]
       start_date: str               (ISO format "YYYY-MM-DD" — strip dashes internally)
       end_date: str                 (ISO format "YYYY-MM-DD" — strip dashes internally)
       periodicity: str              ("DAILY" | "MONTHLY" | "YEARLY")
       returns: long-format DataFrame with columns: security, date, <field1>, <field2>...

     bds(security, field) -> list[str]
       security: str                 (e.g., "KOSPI Index")
       field: str                    (e.g., "INDX_MEMBERS")
       returns: list of ticker strings

     winsorize(arr, lower=0.01, upper=0.01) -> np.ndarray
       arr: array-like (Series or ndarray with possible NaN)
       returns: numpy array with tails clipped at lower/upper quantile limits

     cohens_kappa(rater1, rater2, labels=None) -> tuple[float, float]
       rater1, rater2: list/array of integer codes (0, 1, 2)
       labels: list | None (if None, inferred from union of rater1/rater2)
       returns: (kappa, p_value)

     robust_se(model) -> pd.Series
       model: statsmodels RegressionResultsWrapper fitted with cov_type="HC3"
       returns: Series of robust standard errors indexed by parameter name

     df_to_latex(df, caption, label, footnote=None, float_format="%.3f") -> str
       df: pd.DataFrame
       caption: str
       label: str (LaTeX \label key, e.g., "tab:summary")
       footnote: str | None
       returns: complete standalone .tex table fragment as string
-->

<!-- Key pitfalls from RESEARCH.md:
     Pitfall 1: INDX_MEMBERS sub-element key may not be "Member Ticker and Exchange Code"
       → iterate sub-elements dynamically; log element names during --test
     Pitfall 2: session must be started before getService()
       → check session.start() return value; raise clear RuntimeError if fails
     Pitfall 3: BDH date format is YYYYMMDD, not ISO
       → strip dashes: start_date.replace("-", "")
     Pitfall 4: A2 assumption — test that utils/ is importable from CWD when running
       "python src/script.py"; if not, a note must be added to README
     Pitfall 6: BDP snapshot needs FUNDAMENTAL_DATABASE_DATE override for FY2023 data
       → document in bdp() docstring and in src/01_bloomberg_pull.py (Plan 04)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement utils/bbg.py — Bloomberg wrappers with graceful ImportError</name>
  <files>utils/bbg.py</files>

  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-RESEARCH.md
      (Pattern 1 ImportError guard, Pattern 2 BDP, Pattern 3 BDH, Pattern 4 BDS, Code Examples session setup, Pitfalls 1-3)
    - /Users/dandan/Desktop/Projects/kor-discount/ROADMAP.md (Phase 0 Section 0.2 — bbg.py structure spec)
    - /Users/dandan/Desktop/Projects/kor-discount/utils/__init__.py (current content — do not break package)
  </read_first>

  <action>
Create utils/bbg.py with the following complete implementation:

```python
"""
Bloomberg API wrappers for Korea Discount study.

Exposes three functions that return pandas DataFrames / lists:
  bdp(securities, fields, overrides=None) -> DataFrame
  bdh(securities, fields, start_date, end_date, periodicity="DAILY") -> DataFrame
  bds(security, field) -> list[str]

IMPORTANT: blpapi must be installed at a Bloomberg terminal.
Without blpapi, importing this module is safe (no error), but calling any
function raises RuntimeError with an informative message.

Run connection test:
  python utils/bbg.py --test
"""

import sys
import os

import pandas as pd
from dotenv import load_dotenv

load_dotenv()

try:
    import blpapi
    _BBG_AVAILABLE = True
except ImportError:
    _BBG_AVAILABLE = False

_session = None


def _require_bbg():
    """Raise RuntimeError with install instructions if blpapi is unavailable."""
    if not _BBG_AVAILABLE:
        raise RuntimeError(
            "blpapi is not installed.\n"
            "Run this script at a Bloomberg terminal:\n"
            "  pip install blpapi\n"
            "  python utils/bbg.py --test\n"
        )


def _get_session():
    """Return (and lazily start) the global blpapi session."""
    global _session
    _require_bbg()
    if _session is None:
        so = blpapi.SessionOptions()
        so.setServerHost(os.getenv("BBG_HOST", "127.0.0.1"))
        so.setServerPort(int(os.getenv("BBG_PORT", "8194")))
        _session = blpapi.Session(so)
        if not _session.start():
            raise RuntimeError(
                "Failed to start Bloomberg session. "
                "Check that you are at a Bloomberg terminal and the terminal is logged in."
            )
        if not _session.openService("//blp/refdata"):
            raise RuntimeError("Failed to open Bloomberg refdata service.")
    return _session


def bdp(securities, fields, overrides=None):
    """
    Bloomberg BDP — point-in-time reference data.

    Args:
        securities: str or list of Bloomberg ticker strings (e.g., "005930 KS Equity")
        fields: str or list of Bloomberg field mnemonics (e.g., "PX_TO_BOOK_RATIO")
        overrides: dict of override fieldId -> value pairs, e.g.
                   {"FUNDAMENTAL_DATABASE_DATE": "20231231"} to pin snapshot to FY2023.

    Returns:
        pd.DataFrame with index=securities, columns=fields.
        Missing values represented as None / NaN.
    """
    session = _get_session()
    svc = session.getService("//blp/refdata")
    request = svc.createRequest("ReferenceDataRequest")

    if isinstance(securities, str):
        securities = [securities]
    if isinstance(fields, str):
        fields = [fields]

    for sec in securities:
        request.append("securities", sec)
    for fld in fields:
        request.append("fields", fld)

    if overrides:
        ovrd_elem = request.getElement("overrides")
        for k, v in overrides.items():
            o = ovrd_elem.appendElement()
            o.setElement("fieldId", k)
            o.setElement("value", str(v))

    session.sendRequest(request)

    rows = {}
    while True:
        ev = session.nextEvent(500)
        for msg in ev:
            sec_data = msg.getElement("securityData")
            for i in range(sec_data.numValues()):
                sd = sec_data.getValueAsElement(i)
                ticker = sd.getElementAsString("security")
                fd = sd.getElement("fieldData")
                rows[ticker] = {}
                for fld in fields:
                    if fd.hasElement(fld):
                        try:
                            rows[ticker][fld] = fd.getElementAsString(fld)
                        except Exception:
                            rows[ticker][fld] = None
                    else:
                        rows[ticker][fld] = None
        if ev.eventType() == blpapi.Event.RESPONSE:
            break

    return pd.DataFrame(rows).T


def bdh(securities, fields, start_date, end_date, periodicity="DAILY"):
    """
    Bloomberg BDH — historical time series.

    Args:
        securities: str or list of Bloomberg ticker strings
        fields: str or list of Bloomberg field mnemonics (e.g., "PX_LAST", "RETURN_COM_EQY")
        start_date: ISO date string "YYYY-MM-DD" (dashes stripped internally)
        end_date: ISO date string "YYYY-MM-DD"
        periodicity: "DAILY" | "MONTHLY" | "YEARLY" (default: "DAILY")

    Returns:
        pd.DataFrame in long format with columns:
            security (str), date (datetime.date), <field1>, <field2>, ...
        Batches requests if len(securities) > 100 to avoid throttling.
    """
    import time

    if isinstance(securities, str):
        securities = [securities]
    if isinstance(fields, str):
        fields = [fields]

    # Batch into chunks of 100 to avoid Bloomberg rate limiting
    BATCH_SIZE = 100
    all_frames = []

    for batch_start in range(0, len(securities), BATCH_SIZE):
        batch = securities[batch_start: batch_start + BATCH_SIZE]
        frames = _bdh_batch(batch, fields, start_date, end_date, periodicity)
        all_frames.extend(frames)
        if batch_start + BATCH_SIZE < len(securities):
            time.sleep(0.5)  # small pause between batches

    if not all_frames:
        return pd.DataFrame(columns=["security", "date"] + list(fields))
    return pd.DataFrame(all_frames)


def _bdh_batch(securities, fields, start_date, end_date, periodicity):
    """Internal: run one BDH batch request. Returns list of row dicts."""
    session = _get_session()
    svc = session.getService("//blp/refdata")
    request = svc.createRequest("HistoricalDataRequest")

    for sec in securities:
        request.append("securities", sec)
    for fld in fields:
        request.append("fields", fld)

    # Bloomberg requires YYYYMMDD format — strip dashes
    request.set("startDate", start_date.replace("-", ""))
    request.set("endDate", end_date.replace("-", ""))
    request.set("periodicitySelection", periodicity)

    session.sendRequest(request)

    frames = []
    while True:
        ev = session.nextEvent(500)
        for msg in ev:
            sec_data = msg.getElement("securityData")
            ticker = sec_data.getElementAsString("security")
            fd_array = sec_data.getElement("fieldData")
            for j in range(fd_array.numValues()):
                pt = fd_array.getValueAsElement(j)
                row = {
                    "security": ticker,
                    "date": pt.getElementAsDatetime("date").date(),
                }
                for fld in fields:
                    if pt.hasElement(fld):
                        try:
                            row[fld] = pt.getElementAsFloat(fld)
                        except Exception:
                            row[fld] = None
                    else:
                        row[fld] = None
                frames.append(row)
        if ev.eventType() == blpapi.Event.RESPONSE:
            break

    return frames


def bds(security, field):
    """
    Bloomberg BDS — bulk reference data (array-valued fields).

    Designed for INDX_MEMBERS to pull KOSPI constituents:
        bds("KOSPI Index", "INDX_MEMBERS")

    NOTE: The sub-element name for INDX_MEMBERS tickers varies by Bloomberg
    API version. This implementation tries the known key first, then falls
    back to iterating sub-elements. Verify output at the Bloomberg terminal
    during the connection test (--test mode logs element names).

    Returns:
        list of str — ticker strings from the bulk field.
    """
    session = _get_session()
    svc = session.getService("//blp/refdata")
    request = svc.createRequest("ReferenceDataRequest")
    request.append("securities", security)
    request.append("fields", field)
    session.sendRequest(request)

    results = []
    while True:
        ev = session.nextEvent(500)
        for msg in ev:
            sec_data_elem = msg.getElement("securityData")
            sd = sec_data_elem.getValueAsElement(0)
            fd = sd.getElement("fieldData")
            bulk = fd.getElement(field)
            for i in range(bulk.numValues()):
                elem = bulk.getValueAsElement(i)
                # Try known key first; fall back to first sub-element value
                try:
                    ticker = elem.getElementAsString("Member Ticker and Exchange Code")
                except Exception:
                    # Log element names to help diagnose at terminal
                    names = [
                        elem.getElement(k).name()
                        for k in range(elem.numElements())
                    ]
                    print(
                        f"[bbg.bds] INDX_MEMBERS sub-element keys: {names}. "
                        "Update bds() if 'Member Ticker and Exchange Code' is wrong.",
                        file=sys.stderr,
                    )
                    if elem.numElements() > 0:
                        ticker = elem.getElement(0).getValueAsString()
                    else:
                        continue
                results.append(ticker)
        if ev.eventType() == blpapi.Event.RESPONSE:
            break

    return results


if __name__ == "__main__":
    # python utils/bbg.py --test
    if len(sys.argv) > 1 and sys.argv[1] == "--test":
        if not _BBG_AVAILABLE:
            print(
                "Error: blpapi is not installed.\n"
                "Install at a Bloomberg terminal: pip install blpapi",
                file=sys.stderr,
            )
            sys.exit(1)
        try:
            s = _get_session()
            host = os.getenv("BBG_HOST", "127.0.0.1")
            port = os.getenv("BBG_PORT", "8194")
            print(f"Bloomberg session started OK.")
            print(f"  Host: {host}  Port: {port}")
        except RuntimeError as e:
            print(f"Error: {e}", file=sys.stderr)
            sys.exit(1)
    else:
        print("Usage: python utils/bbg.py --test", file=sys.stderr)
        sys.exit(1)
```
  </action>

  <verify>
    <automated>
      # Import succeeds without blpapi (module-level import guard is safe)
      cd /Users/dandan/Desktop/Projects/kor-discount && python -c "from utils.bbg import bdp, bdh, bds; print('import OK')"
      # --test exits non-zero without blpapi and prints informative message to stderr
      cd /Users/dandan/Desktop/Projects/kor-discount && python utils/bbg.py --test 2>&1; echo "exit: $?"
      # Function signatures exist
      cd /Users/dandan/Desktop/Projects/kor-discount && python -c "import inspect, utils.bbg; print(inspect.signature(utils.bbg.bdp))"
      cd /Users/dandan/Desktop/Projects/kor-discount && python -c "import inspect, utils.bbg; print(inspect.signature(utils.bbg.bdh))"
      cd /Users/dandan/Desktop/Projects/kor-discount && python -c "import inspect, utils.bbg; print(inspect.signature(utils.bbg.bds))"
    </automated>
  </verify>

  <done>
    - utils/bbg.py exists with bdp, bdh, bds functions
    - `from utils.bbg import bdp, bdh, bds` succeeds with no blpapi installed
    - `python utils/bbg.py --test` without blpapi prints an error to stderr and exits non-zero (not a Python traceback)
    - bdp signature: (securities, fields, overrides=None)
    - bdh signature: (securities, fields, start_date, end_date, periodicity="DAILY")
    - bds signature: (security, field)
    - BDH date-stripping (replace("-", "")) is present in the code
    - Batch size of 100 with 0.5s sleep between batches is present in bdh()
    - bds() includes fallback sub-element iteration with stderr logging (Pitfall 1 mitigation)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement utils/stats.py and utils/latex_tables.py</name>
  <files>utils/stats.py, utils/latex_tables.py</files>

  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/01-foundation/01-RESEARCH.md
      (Pattern 5 df_to_latex, Pattern 7 winsorize, cohens_kappa code example, Don't Hand-Roll table)
    - /Users/dandan/Desktop/Projects/kor-discount/ROADMAP.md (Phase 9 Section 9.4 df_to_latex spec)
  </read_first>

  <action>
1. Create utils/stats.py:

```python
"""
Shared statistical utility functions for Korea Discount study.

Functions:
    winsorize(arr, lower=0.01, upper=0.01) -> np.ndarray
    cohens_kappa(rater1, rater2, labels=None) -> tuple[float, float]
    robust_se(model) -> pd.Series
"""

import numpy as np
import pandas as pd
from scipy.stats.mstats import winsorize as _winsorize
from statsmodels.stats.inter_rater import cohens_kappa as _cohens_kappa


def winsorize(arr, lower=0.01, upper=0.01):
    """
    Winsorize array at lower/upper quantile limits.

    Args:
        arr: array-like (pd.Series or np.ndarray); NaN values are propagated.
        lower: lower quantile limit (default 0.01 = 1st percentile)
        upper: upper quantile limit (default 0.01 = 99th percentile)

    Returns:
        np.ndarray with tails clipped. Plain numpy array (not MaskedArray).

    Example:
        winsorize(df["pbr"], lower=0.01, upper=0.01)
    """
    arr = np.asarray(arr, dtype=float)
    # scipy winsorize handles NaN via masked array; convert back to plain ndarray
    return np.array(_winsorize(arr, limits=[lower, upper]))


def cohens_kappa(rater1, rater2, labels=None):
    """
    Compute Cohen's Kappa inter-rater reliability.

    Args:
        rater1: list/array of integer codes (e.g., [0, 1, 2, 1, 0])
        rater2: list/array of integer codes (same length as rater1)
        labels: list of unique codes; if None, inferred from union of rater1 + rater2

    Returns:
        tuple (kappa: float, p_value: float)

    Example:
        kappa, p = cohens_kappa(coder_a_codes, coder_b_codes, labels=[0, 1, 2])
        print(f"κ = {kappa:.3f} (p = {p:.4f})")
    """
    if labels is None:
        labels = sorted(set(list(rater1) + list(rater2)))
    n = len(labels)
    idx = {v: i for i, v in enumerate(labels)}
    table = np.zeros((n, n), dtype=int)
    for a, b in zip(rater1, rater2):
        table[idx[a]][idx[b]] += 1
    result = _cohens_kappa(table)
    return float(result.kappa), float(result.pvalue)


def robust_se(model):
    """
    Extract HC3 robust standard errors from a fitted statsmodels model.

    Args:
        model: statsmodels RegressionResultsWrapper (must be fitted with
               cov_type="HC3", e.g., model.fit(cov_type="HC3"))

    Returns:
        pd.Series of robust standard errors indexed by parameter name.

    Example:
        result = smf.logit("complied ~ pbr + roe", data=df).fit(cov_type="HC3")
        se = robust_se(result)
    """
    return model.bse
```

2. Create utils/latex_tables.py:

```python
"""
DataFrame to LaTeX table exporter for Korea Discount study.

All analysis scripts call df_to_latex() to drop .tex fragments into
outputs/tables/. The paper \input{}s them directly.

Table format: booktabs (\\toprule, \\midrule, \\bottomrule), centered,
with caption, label, and optional footnote in \\footnotesize.
"""

import pandas as pd


def df_to_latex(df, caption, label, footnote=None, float_format="%.3f"):
    """
    Convert a pandas DataFrame to a standalone booktabs LaTeX table fragment.

    Args:
        df: pd.DataFrame to export
        caption: str — table caption (e.g., "Summary Statistics by Compliance Group")
        label: str — LaTeX label key (e.g., "tab:summary") — used as \\label{tab:summary}
        footnote: str | None — optional note in \\footnotesize below the table
        float_format: str — printf-style format for float columns (default "%.3f")

    Returns:
        str — complete standalone .tex table fragment, ready to write to a .tex file
              or pass to \\input{} in main.tex.

    Example:
        tex = df_to_latex(
            summary_df,
            caption="Summary Statistics by Compliance Group",
            label="tab:summary",
            footnote="* p<0.10, ** p<0.05, *** p<0.01. HC3 robust standard errors.",
        )
        with open("outputs/tables/table1_summary.tex", "w") as f:
            f.write(tex)
    """
    col_format = "l" + "r" * len(df.columns)
    body = df.to_latex(
        float_format=float_format,
        escape=False,
        column_format=col_format,
        booktabs=True,
        index=True,
    )
    lines = [
        r"\begin{table}[htbp]",
        r"  \centering",
        rf"  \caption{{{caption}}}",
        rf"  \label{{{label}}}",
        body.strip(),
    ]
    if footnote:
        lines.append(
            r"  \begin{tablenotes}"
            "\n"
            r"    \footnotesize\item \textit{Note:} " + footnote + "\n"
            r"  \end{tablenotes}"
        )
    lines.append(r"\end{table}")
    return "\n".join(lines)
```
  </action>

  <verify>
    <automated>
      cd /Users/dandan/Desktop/Projects/kor-discount && python -c "
from utils.stats import winsorize, cohens_kappa, robust_se
import numpy as np

# winsorize test
arr = list(range(100))
result = winsorize(arr, lower=0.01, upper=0.01)
assert isinstance(result, np.ndarray), 'winsorize must return ndarray'
print('winsorize: OK')

# cohens_kappa test
kappa, pval = cohens_kappa([0,1,2,0,1], [0,1,2,1,0], labels=[0,1,2])
assert isinstance(kappa, float), 'kappa must be float'
assert isinstance(pval, float), 'pval must be float'
print(f'cohens_kappa: OK (kappa={kappa:.3f})')
"
      cd /Users/dandan/Desktop/Projects/kor-discount && python -c "
from utils.latex_tables import df_to_latex
import pandas as pd

df = pd.DataFrame({'mean': [1.23, 4.56], 'sd': [0.11, 0.22]}, index=['Group A', 'Group B'])
tex = df_to_latex(df, 'Test Table', 'tab:test', footnote='Test note.')
assert r'\begin{table}' in tex, 'missing table env'
assert r'\toprule' in tex, 'missing toprule'
assert r'\caption{Test Table}' in tex, 'missing caption'
assert r'\label{tab:test}' in tex, 'missing label'
print('df_to_latex: OK')
print(tex[:200])
"
    </automated>
  </verify>

  <done>
    - utils/stats.py exports winsorize, cohens_kappa, robust_se
    - winsorize(list(range(100)), lower=0.01, upper=0.01) returns np.ndarray without error
    - cohens_kappa([0,1,2,0,1], [0,1,2,1,0]) returns (float, float) tuple
    - utils/latex_tables.py exports df_to_latex
    - df_to_latex(df, caption, label) returns string containing \begin{table}, \toprule, \caption{...}, \label{...}
    - All three utils modules importable together: `from utils.stats import winsorize; from utils.latex_tables import df_to_latex`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blpapi session → Bloomberg terminal | Session data comes from Bloomberg over localhost; trust is implicit at terminal |
| env vars → bbg.py | BBG_HOST and BBG_PORT read from .env; malformed values cause session failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | _get_session() host/port | accept | Terminal-only usage; host is always localhost in practice; low risk |
| T-02-02 | Denial of Service | bdh() large universe request | mitigate | Batch size 100 + 0.5s sleep between batches prevents Bloomberg throttle |
| T-02-03 | Information Disclosure | Error messages in bbg.py | accept | Error messages reference install instructions only; no credentials exposed |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/dandan/Desktop/Projects/kor-discount

# All utils imports work
python -c "from utils.bbg import bdp, bdh, bds; from utils.stats import winsorize, cohens_kappa; from utils.latex_tables import df_to_latex; print('All imports OK')"

# bbg.py --test fails gracefully without blpapi
python utils/bbg.py --test 2>&1 | grep -i "blpapi\|bloomberg\|error" && echo "graceful error: OK"
echo "exit was: $?"

# winsorize returns ndarray
python -c "import numpy as np; from utils.stats import winsorize; r=winsorize(list(range(100))); assert type(r).__name__=='ndarray'; print('winsorize ndarray: OK')"

# df_to_latex contains booktabs markers
python -c "import pandas as pd; from utils.latex_tables import df_to_latex; tex=df_to_latex(pd.DataFrame({'a':[1]}), 'T', 'tab:t'); assert chr(92)+'toprule' in tex; print('booktabs: OK')"
```
</verification>

<success_criteria>
1. `from utils.bbg import bdp, bdh, bds` exits 0 (no blpapi needed for import)
2. `python utils/bbg.py --test` without blpapi: prints "blpapi" or "Bloomberg terminal" in stderr output and exits non-zero — NOT a Python traceback
3. bdp, bdh, bds function signatures match the interface spec in this plan's context block
4. bdh() contains batch size 100 loop with sleep between batches
5. bds() contains fallback sub-element iteration (Pitfall 1 mitigation)
6. winsorize(arr) returns np.ndarray
7. cohens_kappa(r1, r2) returns (float, float)
8. df_to_latex(df, caption, label) output contains: \begin{table}, \toprule, \caption{}, \label{}
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
