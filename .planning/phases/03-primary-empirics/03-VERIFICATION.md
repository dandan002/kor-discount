---
phase: 03-primary-empirics
verified: 2026-04-20T20:53:54Z
status: gaps_found
score: 21/24 must-haves verified
overrides_applied: 0
gaps:
  - truth: "The stacked event study estimates cumulative abnormal valuation changes separately for each Japan reform date with heteroskedasticity-robust standard errors"
    status: failed
    reason: "The event-study script calls HC3, but the generated non-baseline inference columns are entirely blank because the cohort x relative-time design is saturated. The table still labels those columns as HC3 SE/t-stat/p-value."
    artifacts:
      - path: "src/analysis/event_study.py"
        issue: "estimate_event_study creates one dummy per cohort-month cell and suppresses the RuntimeWarning generated by undefined HC3 diagnostics."
      - path: "output/tables/event_study_car.csv"
        issue: "All 108 non-baseline rows have null hc3_se, t_stat, and p_value."
      - path: "output/tables/table_event_study_coefs.tex"
        issue: "The table claims HC3 robust standard errors but renders -- for non-baseline HC3 SE/t-stat/p-value cells."
    missing:
      - "Change the event-study estimator so HC3 inference is estimable, or relabel the output as descriptive CARs and remove the robust-inference claim."
      - "Add a test that non-baseline event-study inference fields are populated when EVNT-02 remains in scope."
  - truth: "Panel OLS regression table is LaTeX-formatted with country-clustered inference for reform-interaction dummies"
    status: partial
    reason: "PanelOLS and wild-bootstrap p-values exist in the machine-readable CSV, but the displayed standard errors are PanelOLS robust standard errors, not country-clustered standard errors, and Table 2 does not display the wild-bootstrap p-values referenced in its note."
    artifacts:
      - path: "src/analysis/panel_ols.py"
        issue: "PanelOLS is fit with cov_type='robust' rather than a country-clustered covariance; wild-bootstrap p-values are computed separately."
      - path: "output/tables/table2_ols.tex"
        issue: "Cells show coef (std_error) only, while the note advertises wild-bootstrap p-values clustered by country."
    missing:
      - "Use country-clustered standard errors in PanelOLS or clearly present the wild-bootstrap inference in Table 2."
      - "Update tests to assert the displayed regression table contains the intended clustered or bootstrap inference."
deferred:
  - truth: "GEO-03 final integration into the paper's causal mechanism section"
    addressed_in: "Phase 5"
    evidence: "Phase 5 goal: a complete LaTeX paper integrating all programmatically generated figures and tables; PAPER-06 covers the causal mechanism section."
---

# Phase 3: Primary Empirics Verification Report

**Phase Goal:** Event study, panel OLS, and geopolitical risk sub-analysis are fully estimated - results tables and figures exist and are ready for inclusion in the paper
**Verified:** 2026-04-20T20:53:54Z
**Status:** gaps_found
**Re-verification:** No - initial verification

## Goal Achievement

Phase 3 generated the expected scripts, figures, CSV files, and LaTeX tables, and the runnable gates pass. The phase goal is not fully achieved because two inference artifacts are not ready for paper inclusion: event-study HC3 inference is blank, and Panel OLS table inference does not satisfy the country-clustered/wild-bootstrap presentation contract.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Roadmap SC1: stacked event study estimates CARs for three Japan reform dates with heteroskedasticity-robust standard errors | FAILED | `event_study_car.csv` has 111 rows and three cohorts, but all 108 non-baseline rows have null `hc3_se`, `t_stat`, and `p_value`; `table_event_study_coefs.tex:7-14` claims HC3 but renders `--`. |
| 2 | Roadmap SC2: PanelOLS estimates reform interactions and Table 2 presents country-clustered/wild-bootstrap inference | PARTIAL | `panel_ols.py` uses `PanelOLS` and computes wild p-values, but line 105 fits `cov_type="robust"` and `table2_ols.tex:12-15` does not display wild p-values. |
| 3 | Roadmap SC3: GPR escalation indicator and KOSPI valuation response estimated with caveats | VERIFIED | `geo_risk.py` loads `GPRC_KOR`, computes the 75th percentile threshold, estimates `kospi_pb ~ gpr_escalation_dummy + topix_pb + C(year)`, and writes caveated CSV/LaTeX outputs. |
| 4 | Roadmap SC4: analyses read canonical local inputs and analysis modules do not import one another | VERIFIED | All scripts read `config.PROCESSED_DIR / "panel.parquet"`; geo-risk also reads local `config.RAW_DIR / GPR_FILENAME`; isolation pytest passed. |
| 5 | Phase 3 tests are discoverable before implementation begins | VERIFIED | `pytest tests/test_phase3.py --collect-only -q` is covered by the scaffold and full phase tests now pass. |
| 6 | GPR source file exists locally and is documented | VERIFIED | `data/raw/data_gpr_export.xls` exists; manifest line 144 records the Caldara-Iacoviello source URL. |
| 7 | `requirements.txt` pins `xlrd==2.0.1` | VERIFIED | `requirements.txt:14` contains `xlrd==2.0.1`; workbook read succeeded through geo-risk script execution. |
| 8 | `src/analysis` exists as a package | VERIFIED | `src/analysis/__init__.py` exists; analysis modules import in tests. |
| 9 | `python src/analysis/event_study.py` produces Figure 2 and event-study tables from `panel.parquet` | VERIFIED | Command exited 0 and saved `figure2_event_study.pdf`, `event_study_car.csv`, and `table_event_study_coefs.tex`. |
| 10 | Event study uses reform dates from `config.py` and avoids hardcoded date literals in the script | VERIFIED | `event_study.py` references `config.EVENT_DATES` and `config.EVENT_LABELS`; no hardcoded 2014/2015/2023 date strings were found in the script. |
| 11 | Event-study windows and abnormal valuation construction are implemented | VERIFIED | `build_stacked_dataset()` returned three cohorts with -36..+24 rows, 36 pre-event months per cohort, and overlap annotations. |
| 12 | Stacked design preserves full windows, flags overlap, and provides HC3 standard errors | FAILED | Window preservation and overlap flags pass, but HC3 output fields are null for every non-baseline event month. |
| 13 | `python src/analysis/panel_ols.py` estimates PanelOLS models with country and time fixed effects | VERIFIED | Command exited 0; source uses `PanelOLS(..., entity_effects=True, time_effects=True)`. |
| 14 | Panel OLS includes reform dummies interacted with Japan/TOPIX | VERIFIED | Source constructs `stewardship_x_japan`, `cgc_x_japan`, and `tse_pb_reform_x_japan`; CSV has coefficients for all three. |
| 15 | Wild-bootstrap inference is clustered by country with 999 Rademacher draws | VERIFIED | Source calls `wildboottest(..., B=BOOTSTRAP_ITERATIONS, cluster=country_cluster, weights_type="rademacher")`; CSV has three non-null `wild_p_value` values. |
| 16 | Table 2 is saved with booktabs rules and two-decimal displayed coefficients | VERIFIED | `table2_ols.tex` exists with `\toprule`, `\midrule`, and two-decimal coefficient cells. |
| 17 | `python src/analysis/geo_risk.py` constructs Korea GPR escalation dummy from 2004-2024 `GPRC_KOR` | VERIFIED | Command exited 0; log reports threshold 0.3699, share 0.250, N=252. |
| 18 | GPR escalation dummy equals 1 above the 75th percentile over 2004-2024 | VERIFIED | Source filters to `STUDY_START` and `STUDY_END` before `quantile(0.75)` and uses `>` threshold. |
| 19 | Geo-risk regression estimates KOSPI P/B on escalation dummy, year FE, and TOPIX P/B | VERIFIED | Source formula is `kospi_pb ~ gpr_escalation_dummy + topix_pb + C(year)` with HC3 covariance. |
| 20 | Geo-risk outputs include Figure 3 and caveated result artifacts | VERIFIED | `figure3_geo_risk.pdf`, `table3_geo_risk.tex`, and `geo_risk_results.csv` exist; table and CSV include partial-identification caveat text. |
| 21 | All Phase 3 scripts run from a clean command line without importing one another | VERIFIED | All three scripts exited 0; `pytest tests/test_phase3.py::test_analysis_modules_do_not_import_each_other -x -q` passed. |
| 22 | `pytest tests/test_phase3.py -x -q` passes after outputs exist | VERIFIED | Command passed: 12 tests. |
| 23 | `pytest tests/ -x -q` passes after Phase 3 outputs are generated | VERIFIED | Command passed: 19 tests. |
| 24 | All required Phase 3 paper artifacts exist under `output/figures` and `output/tables` | VERIFIED | Figure 2, Figure 3, Table 2, event-study coefficient table, and Table 3 are non-empty. |

**Score:** 21/24 truths verified

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|--------------|----------|
| 1 | GEO-03 final integration into the paper's causal mechanism section | Phase 5 | Phase 5 goal covers a complete LaTeX paper integrating generated figures and tables; PAPER-06 covers causal mechanism prose. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_phase3.py` | Phase 3 Nyquist tests | VERIFIED | 184 lines; 12 collected/passing tests, including the added Panel OLS CSV contract test. |
| `data/raw/data_gpr_export.xls` | Local Caldara-Iacoviello GPR workbook | VERIFIED | Present and read by `geo_risk.py` using `engine="xlrd"`. |
| `data/raw/MANIFEST.md` | Raw-data provenance | VERIFIED | Contains `data_gpr_export.xls` and exact source URL at line 144. |
| `requirements.txt` | `xlrd==2.0.1` pin | VERIFIED | Pin present at line 14. |
| `src/analysis/event_study.py` | Standalone event-study script | PARTIAL | Substantive and wired, but estimator produces blank non-baseline HC3 diagnostics. |
| `output/figures/figure2_event_study.pdf` | Event-study figure | VERIFIED | Non-empty PDF, 22167 bytes after regeneration. |
| `output/tables/event_study_car.csv` | Cohort x relative-month CAR output | HOLLOW | Has required columns and coverage, but non-baseline HC3/t/p fields are all null. |
| `output/tables/table_event_study_coefs.tex` | Event-study coefficient table | HOLLOW | Non-empty LaTeX table, but HC3/t/p cells render `--` for non-baseline rows. |
| `src/analysis/panel_ols.py` | Standalone PanelOLS script | PARTIAL | PanelOLS and wild p-values are implemented, but displayed SEs are not country-clustered. |
| `output/tables/panel_ols_results.csv` | Machine-readable PanelOLS output | VERIFIED | Contains reform-interaction coefficients and non-null wild p-values. |
| `output/tables/table2_ols.tex` | Panel OLS paper table | PARTIAL | Booktabs table exists, but does not display the wild-bootstrap p-values referenced by the note and uses robust SEs. |
| `src/analysis/geo_risk.py` | Standalone geo-risk script | VERIFIED | Substantive, wired to local GPR and canonical panel inputs. |
| `output/figures/figure3_geo_risk.pdf` | Geo-risk figure | VERIFIED | Non-empty PDF, 27962 bytes after regeneration. |
| `output/tables/table3_geo_risk.tex` | Geo-risk regression table | VERIFIED | Contains booktabs rules and partial-identification caveat. |
| `output/tables/geo_risk_results.csv` | Geo-risk machine-readable output | VERIFIED | Contains threshold, escalation coefficient, TOPIX control, and caveat note. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `tests/test_phase3.py` | `config.OUTPUT_DIR` | Output constants | WIRED | Lines 18-19 derive figures/tables from `config.OUTPUT_DIR`. |
| `tests/test_phase3.py` | GPR raw file | `GPR_PATH = config.RAW_DIR / "data_gpr_export.xls"` | WIRED | Line 21 uses the local raw-data path. |
| `data/raw/MANIFEST.md` | Caldara-Iacoviello source | Source URL row | WIRED | Line 144 contains the exact source URL. |
| `src/analysis/event_study.py` | `config.EVENT_DATES` | Event loop and cohort labels | WIRED | Lines 83, 202, 229, and 296 reference `config.EVENT_DATES`. |
| `src/analysis/event_study.py` | `data/processed/panel.parquet` | `pd.read_parquet(config.PROCESSED_DIR / "panel.parquet")` | WIRED | Line 330 reads canonical panel. |
| `src/analysis/event_study.py` | HC3 robust SE | `get_robustcov_results(cov_type="HC3")` | HOLLOW | The call exists at line 221, but output SE/t/p fields are null. |
| `src/analysis/panel_ols.py` | `linearmodels.PanelOLS` | Two-way FE model | WIRED | Lines 97-105 instantiate PanelOLS with entity and time effects. |
| `src/analysis/panel_ols.py` | wild bootstrap | `wildboottest` | WIRED | Lines 189-197 call `wildboottest` with country cluster labels. |
| `src/analysis/panel_ols.py` | Table 2 output | `output_path.write_text` | PARTIAL | Table is written, but inference display is incomplete for OLS-03. |
| `src/analysis/geo_risk.py` | GPR workbook | `pd.read_excel(config.RAW_DIR / GPR_FILENAME, engine="xlrd")` | WIRED | Lines 48-53 read fixed local workbook. |
| `src/analysis/geo_risk.py` | `data/processed/panel.parquet` | `pd.read_parquet(config.PROCESSED_DIR / "panel.parquet")` | WIRED | Line 253 reads canonical panel. |
| `src/analysis/geo_risk.py` | partial-identification caveat | LaTeX and CSV notes | WIRED | Caveat appears in source, `table3_geo_risk.tex`, and `geo_risk_results.csv`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `event_study.py` | `panel`, `stacked`, `car` | `data/processed/panel.parquet` via `pd.read_parquet` | Yes for CAR coefficients and figures | PARTIAL - inference fields hollow |
| `event_study_car.csv` | `hc3_se`, `t_stat`, `p_value` | Statsmodels HC3 result | No usable non-baseline values | HOLLOW |
| `panel_ols.py` | `results_df`, `wild_p_value` | `panel.parquet`, PanelOLS, wildboottest | Yes in CSV | PARTIAL - Table 2 omits wild p-values and SE covariance is robust, not clustered |
| `geo_risk.py` | `gpr_escalation_dummy`, `reg_df`, `result` | Local GPR workbook plus `panel.parquet` | Yes | VERIFIED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Regenerate event-study outputs | `python src/analysis/event_study.py` | Exited 0; saved CSV, LaTeX, and Figure 2 | PASS |
| Regenerate Panel OLS outputs | `python src/analysis/panel_ols.py` | Exited 0; saved CSV and Table 2; warning about full enumeration with four clusters | PASS |
| Regenerate geo-risk outputs | `python src/analysis/geo_risk.py` | Exited 0; threshold 0.3699, share 0.250, N=252 | PASS |
| Phase 3 tests | `pytest tests/test_phase3.py -x -q` | 12 passed | PASS |
| Full test suite | `pytest tests/ -x -q` | 19 passed | PASS |
| Analysis module isolation | `pytest tests/test_phase3.py::test_analysis_modules_do_not_import_each_other -x -q` | 1 passed | PASS |
| Event-study inference content | `pd.read_csv(...); non_base[['hc3_se','t_stat','p_value']].notna().sum()` | 0 populated values for each field across 108 non-baseline rows | FAIL |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| EVNT-01 | 03-01, 03-02, 03-05 | Run event study around three Japan reform dates | SATISFIED | Script runs and outputs three cohorts for 2014, 2015, and 2023. |
| EVNT-02 | 03-01, 03-02, 03-05 | Event study uses heteroskedasticity-robust standard errors, presented separately | BLOCKED | HC3 call exists, but generated HC3 SE/t/p columns are blank for all non-baseline rows. |
| EVNT-03 | 03-01, 03-02, 03-05 | Stacked event-study design across treatment dates | SATISFIED | `build_stacked_dataset()` returns three full -36..+24 cohorts with overlap annotations and Cengiz comment. |
| EVNT-04 | 03-01, 03-02, 03-05 | CAR figures and coefficient tables for paper | PARTIAL | Figure and tables exist, but coefficient table is not paper-ready while HC3 fields are blank. |
| OLS-01 | 03-01, 03-03, 03-05 | Two-way country + time FE using `linearmodels.PanelOLS` | SATISFIED | Source imports PanelOLS and fits entity/time effects. |
| OLS-02 | 03-01, 03-03, 03-05 | Reform dummies interacted with Japan indicator | SATISFIED | Source constructs three interaction terms; CSV reports coefficients. |
| OLS-03 | 03-01, 03-03, 03-05 | Standard errors clustered by country and regression table presented | BLOCKED | `model.fit(cov_type="robust")` is not country-clustered; Table 2 omits displayed wild-bootstrap p-values. |
| GEO-01 | 03-01, 03-04, 03-05 | Construct North Korea escalation indicator from GPR | SATISFIED | GPRC_KOR loaded from local workbook and 75th percentile dummy created. |
| GEO-02 | 03-01, 03-04, 03-05 | Estimate KOSPI valuation response to escalation events | SATISFIED | HC3 geo-risk regression output exists in CSV and LaTeX. |
| GEO-03 | 03-01, 03-04, 03-05 | Results integrated with caveats about partial identification | SATISFIED FOR PHASE 3 / PAPER INTEGRATION DEFERRED | Phase 3 artifacts contain caveat text; final narrative integration belongs to Phase 5 paper assembly. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/analysis/event_study.py` | 214-221 | Suppressed RuntimeWarning around HC3 robust covariance | Blocker | Masks the undefined-inference condition that leaves HC3 outputs blank. |
| `output/tables/event_study_car.csv` | data check | Hard-empty inference fields | Blocker | Fails EVNT-02 and makes the event-study coefficient table not paper-ready. |
| `tests/test_phase3.py` | 71-74 | Test only checks strings `HC3` and `CAR` | Warning | Passing test does not verify that HC3 inference values exist. |
| `src/analysis/panel_ols.py` | 105 | `cov_type="robust"` | Blocker | Does not satisfy country-clustered standard-error requirement. |
| `output/tables/table2_ols.tex` | 12-15 | Bootstrap note without displayed bootstrap p-values | Warning | Paper table may mislead readers about which inference is displayed. |

### Human Verification Required

Automated verification already found blocking gaps, so status remains `gaps_found`. After the inference gaps are fixed, a human should visually inspect `figure2_event_study.pdf` and `figure3_geo_risk.pdf` for paper layout quality, axis readability, and caption fit.

### Gaps Summary

Two root gaps block phase goal achievement:

1. Event-study outputs are generated, but robust inference is hollow. The script calls HC3, yet the saturated design produces no usable non-baseline standard errors, t-statistics, or p-values. This blocks EVNT-02 and makes the event-study coefficient table not ready for paper inclusion.

2. Panel OLS outputs are generated, but the regression table does not satisfy the country-clustered inference contract. The PanelOLS model uses robust covariance, wild-bootstrap p-values are only in the CSV, and Table 2 displays coefficient cells without the wild-bootstrap p-values it references.

No later roadmap phase explicitly fixes these inference artifacts, so they are actionable Phase 3 gaps.

---

_Verified: 2026-04-20T20:53:54Z_
_Verifier: Claude (gsd-verifier)_
