---
phase: 03-primary-empirics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/analysis/__init__.py
  - tests/test_phase3.py
  - data/raw/data_gpr_export.xls
  - data/raw/MANIFEST.md
autonomous: true
requirements:
  - EVNT-01
  - EVNT-02
  - EVNT-03
  - EVNT-04
  - OLS-01
  - OLS-02
  - OLS-03
  - GEO-01
  - GEO-02
  - GEO-03

must_haves:
  truths:
    - "Phase 3 pytest tests are discoverable before implementation begins"
    - "The GPR source file exists locally at data/raw/data_gpr_export.xls and is documented in data/raw/MANIFEST.md"
    - "requirements.txt pins xlrd==2.0.1 so pandas can read the Caldara-Iacoviello .xls file"
    - "src/analysis exists as a package for standalone Phase 3 scripts"
  artifacts:
    - path: "tests/test_phase3.py"
      provides: "Nyquist tests for EVNT-01 through GEO-03"
      exports: ["test_three_cohorts", "test_figure2_exists", "test_figure2_panels", "test_event_study_coefs", "test_table2_exists", "test_table2_reform_dummies", "test_table2_booktabs", "test_gpr_threshold", "test_figure3_exists", "test_geo_caveats", "test_analysis_modules_do_not_import_each_other"]
    - path: "data/raw/data_gpr_export.xls"
      provides: "Caldara-Iacoviello country-level GPR raw data export"
    - path: "data/raw/MANIFEST.md"
      provides: "Provenance for data_gpr_export.xls"
      contains: "https://www.matteoiacoviello.com/gpr_files/data_gpr_export.xls"
    - path: "src/analysis/__init__.py"
      provides: "Package marker for Phase 3 standalone scripts"
  key_links:
    - from: "tests/test_phase3.py"
      to: "config.OUTPUT_DIR"
      via: "tests derive output/figures and output/tables from config.OUTPUT_DIR"
      pattern: "config\\.OUTPUT_DIR"
    - from: "tests/test_phase3.py"
      to: "data/raw/data_gpr_export.xls"
      via: "pd.read_excel(..., engine='xlrd')"
      pattern: "data_gpr_export\\.xls"
    - from: "data/raw/MANIFEST.md"
      to: "Caldara-Iacoviello GPR source"
      via: "provenance row with source URL"
      pattern: "matteoiacoviello\\.com/gpr_files/data_gpr_export\\.xls"
---

<objective>
Create the Phase 3 validation and data foundation.

Purpose: Make every Phase 3 empirical script test-driven from the start, and make the GPR raw data reproducible and provenance-tracked before the geopolitical analysis runs.

Output: `tests/test_phase3.py`, `src/analysis/__init__.py`, pinned `xlrd==2.0.1`, downloaded `data/raw/data_gpr_export.xls`, and a manifest entry with the exact Caldara-Iacoviello source URL.
</objective>

<execution_context>
@/Users/dandan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dandan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/ROADMAP.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/REQUIREMENTS.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-CONTEXT.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-RESEARCH.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-VALIDATION.md
@/Users/dandan/Desktop/Projects/kor-discount/config.py
@/Users/dandan/Desktop/Projects/kor-discount/requirements.txt
@/Users/dandan/Desktop/Projects/kor-discount/data/raw/MANIFEST.md
@/Users/dandan/Desktop/Projects/kor-discount/tests/test_descriptive.py

<interfaces>
Config constants used by tests:
```python
PROCESSED_DIR: Path = PROJECT_ROOT / "data" / "processed"
RAW_DIR: Path = PROJECT_ROOT / "data" / "raw"
OUTPUT_DIR: Path = PROJECT_ROOT / "output"
EVENT_DATES: list[datetime.date]
EVENT_LABELS: dict[datetime.date, str]
```

Required GPR source:
```text
data/raw/data_gpr_export.xls
https://www.matteoiacoviello.com/gpr_files/data_gpr_export.xls
GPR column: GPRC_KOR
Date column: month
Study window for threshold: 2004-01-01 through 2024-12-31
```

Test bootstrap pattern:
```python
PROJECT_ROOT = Path(__file__).resolve().parents[1]
if str(PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(PROJECT_ROOT))
import config
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin xlrd, create analysis package, and register GPR provenance</name>
  <files>requirements.txt, src/analysis/__init__.py, data/raw/data_gpr_export.xls, data/raw/MANIFEST.md</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/requirements.txt (file being modified; verify whether xlrd is already pinned)
    - /Users/dandan/Desktop/Projects/kor-discount/data/raw/MANIFEST.md (file being modified; preserve existing Bloomberg provenance table)
    - /Users/dandan/Desktop/Projects/kor-discount/src/descriptive/figure1.py (script package/output directory pattern)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-CONTEXT.md (D-09 and D-15 source and standalone-module decisions)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-RESEARCH.md (GPR source URL and xlrd engine guidance)
  </read_first>
  <action>
Create `src/analysis/__init__.py` as an empty file.

Update `requirements.txt` by adding exactly this pinned dependency under the existing data/statistics dependencies if it is not already present:

```text
xlrd==2.0.1
```

Download the Caldara-Iacoviello GPR export into the project raw data directory:

```bash
curl -L -o data/raw/data_gpr_export.xls https://www.matteoiacoviello.com/gpr_files/data_gpr_export.xls
```

Append this exact section to `data/raw/MANIFEST.md` if no `data_gpr_export.xls` entry already exists:

```markdown

## External Research Data

| File | Source | Source URL | Vintage Date | Download Method | Notes |
|------|--------|------------|--------------|-----------------|-------|
| data_gpr_export.xls | Caldara-Iacoviello Geopolitical Risk (GPR) country-level data export | https://www.matteoiacoviello.com/gpr_files/data_gpr_export.xls | 2026-04-18 | curl -L -o data/raw/data_gpr_export.xls https://www.matteoiacoviello.com/gpr_files/data_gpr_export.xls | Contains the GPRC_KOR monthly Korea country-level series used for Phase 3 GEO-01 through GEO-03. |
```

Do not add any database schema task. Do not make any Phase 3 analysis script download data at runtime; normal analysis runs must read the local `data/raw/data_gpr_export.xls` file only.
  </action>
  <acceptance_criteria>
    - `grep -q "xlrd==2.0.1" requirements.txt` exits 0.
    - `test -f src/analysis/__init__.py` exits 0.
    - `test -f data/raw/data_gpr_export.xls` exits 0.
    - `grep -q "data_gpr_export.xls" data/raw/MANIFEST.md` exits 0.
    - `grep -q "https://www.matteoiacoviello.com/gpr_files/data_gpr_export.xls" data/raw/MANIFEST.md` exits 0.
    - `python -c "import pandas as pd; df = pd.read_excel('data/raw/data_gpr_export.xls', usecols=['month','GPRC_KOR'], engine='xlrd'); assert len(df) > 0; print('GPR XLS OK')"` prints `GPR XLS OK`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && python -c "import pandas as pd; df = pd.read_excel('data/raw/data_gpr_export.xls', usecols=['month','GPRC_KOR'], engine='xlrd'); assert len(df) > 0; print('GPR XLS OK')"</automated>
  </verify>
  <done>The GPR .xls file is present, readable with xlrd, and provenance is documented with the exact source URL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Phase 3 Nyquist test scaffold</name>
  <files>tests/test_phase3.py</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase3.py (file being modified or created)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_descriptive.py (existing pytest path/bootstrap pattern)
    - /Users/dandan/Desktop/Projects/kor-discount/config.py (output/data constants and event dates)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-VALIDATION.md (required test names and commands)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-CONTEXT.md (D-01 through D-15 decisions tested here)
  </read_first>
  <behavior>
    - `pytest tests/test_phase3.py --collect-only -q` collects all Phase 3 tests without import-time errors.
    - `test_three_cohorts` imports `src.analysis.event_study` inside the test body, calls `build_stacked_dataset(panel_df)`, and asserts exactly 3 distinct cohorts with relative time bounded by -36 and 24.
    - `test_figure2_exists` checks `output/figures/figure2_event_study.pdf` exists and has size greater than 0.
    - `test_figure2_panels` reads `output/tables/event_study_car.csv` and asserts exactly 3 unique cohorts and event labels.
    - `test_event_study_coefs` reads `output/tables/table_event_study_coefs.tex` and asserts `HC3` and `CAR` appear.
    - `test_table2_exists`, `test_table2_reform_dummies`, and `test_table2_booktabs` validate `output/tables/table2_ols.tex`.
    - `test_gpr_threshold` imports `src.analysis.geo_risk`, calls `load_gpr_korea()`, and asserts the escalation share is between 0.20 and 0.30.
    - `test_figure3_exists` checks `output/figures/figure3_geo_risk.pdf` exists and has size greater than 0.
    - `test_geo_caveats` asserts `output/tables/table3_geo_risk.tex` contains `partial identification` or `caveat` case-insensitively.
    - `test_analysis_modules_do_not_import_each_other` parses `src/analysis/event_study.py`, `src/analysis/panel_ols.py`, and `src/analysis/geo_risk.py` when they exist and fails if any imports from another `src.analysis.*` module.
  </behavior>
  <action>
Create `tests/test_phase3.py` using the Phase 2 pytest bootstrap pattern. The file must define these exact constants:

```python
OUTPUT_FIGURES = config.OUTPUT_DIR / "figures"
OUTPUT_TABLES = config.OUTPUT_DIR / "tables"
PANEL_PATH = config.PROCESSED_DIR / "panel.parquet"
GPR_PATH = config.RAW_DIR / "data_gpr_export.xls"
```

Create these exact test functions:

```python
def test_three_cohorts(): ...
def test_figure2_exists(): ...
def test_figure2_panels(): ...
def test_event_study_coefs(): ...
def test_table2_exists(): ...
def test_table2_reform_dummies(): ...
def test_table2_booktabs(): ...
def test_gpr_threshold(): ...
def test_figure3_exists(): ...
def test_geo_caveats(): ...
def test_analysis_modules_do_not_import_each_other(): ...
```

All imports of `src.analysis.event_study`, `src.analysis.panel_ols`, and `src.analysis.geo_risk` must occur inside test functions so collection works before those modules exist. `test_analysis_modules_do_not_import_each_other` must use `ast.parse` and fail on `ImportFrom` nodes whose module starts with `src.analysis.` or `analysis.` and names a different analysis module.

The first full run of `pytest tests/test_phase3.py -x -q` may fail because output-generating scripts do not exist yet; the required foundation verification is collection success.
  </action>
  <acceptance_criteria>
    - `grep -q "def test_three_cohorts" tests/test_phase3.py` exits 0.
    - `grep -q "def test_analysis_modules_do_not_import_each_other" tests/test_phase3.py` exits 0.
    - `grep -q "event_study_car.csv" tests/test_phase3.py` exits 0.
    - `grep -q "table2_ols.tex" tests/test_phase3.py` exits 0.
    - `grep -q "table3_geo_risk.tex" tests/test_phase3.py` exits 0.
    - `pytest tests/test_phase3.py --collect-only -q` collects 11 tests and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && pytest tests/test_phase3.py --collect-only -q</automated>
  </verify>
  <done>`tests/test_phase3.py` is present, collects 11 tests, and covers every Phase 3 requirement ID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> data/raw | A remote `.xls` file is downloaded once from matteoiacoviello.com and stored as raw research data. |
| data/raw -> tests | Tests read the local `.xls` file with pandas/xlrd to validate schema. |
| local filesystem -> output | No generated output is produced in this plan except test/provenance files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `data/raw/data_gpr_export.xls` | mitigate | Store the raw file in `data/raw/`, document the exact source URL in `data/raw/MANIFEST.md`, and verify readable columns `month` and `GPRC_KOR` immediately after download. |
| T-03-01-02 | Repudiation | `data/raw/MANIFEST.md` | mitigate | Append a provenance row with file name, source, source URL, vintage date `2026-04-18`, download command, and Phase 3 usage notes. |
| T-03-01-03 | Denial of Service | `pd.read_excel(..., engine='xlrd')` | mitigate | Pin `xlrd==2.0.1` in `requirements.txt` and verify the file loads before analysis tasks start. |
| T-03-01-04 | Information Disclosure | GPR raw/output files | accept | The dataset is public research data and contains no PII or secrets. |
| T-03-01-05 | High severity appsec | Phase 3 foundation | accept | No high-severity appsec threat applies: this is local research code with public data, no auth boundary, no server, and no secret handling. |
</threat_model>

<verification>
- `python -c "import pandas as pd; df = pd.read_excel('data/raw/data_gpr_export.xls', usecols=['month','GPRC_KOR'], engine='xlrd'); assert len(df) > 0; print('GPR XLS OK')"` exits 0.
- `pytest tests/test_phase3.py --collect-only -q` exits 0 and collects 11 tests.
- `pytest tests/test_phase3.py -x -q` is expected to fail until Wave 2 scripts generate outputs.
</verification>

<success_criteria>
- `requirements.txt` contains `xlrd==2.0.1`.
- `data/raw/data_gpr_export.xls` exists and is readable with pandas using `engine="xlrd"`.
- `data/raw/MANIFEST.md` contains `data_gpr_export.xls` and `https://www.matteoiacoviello.com/gpr_files/data_gpr_export.xls`.
- `tests/test_phase3.py` contains all 11 named tests and collects without errors.
- No Phase 3 analysis module imports another Phase 3 analysis module.
</success_criteria>

<output>
After completion, create `.planning/phases/03-primary-empirics/03-01-SUMMARY.md`.
</output>
