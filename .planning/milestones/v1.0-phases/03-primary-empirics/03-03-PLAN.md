---
phase: 03-primary-empirics
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/analysis/panel_ols.py
  - output/tables/table2_ols.tex
  - output/tables/panel_ols_results.csv
autonomous: true
requirements:
  - OLS-01
  - OLS-02
  - OLS-03

must_haves:
  truths:
    - "Running `python src/analysis/panel_ols.py` estimates PanelOLS models using country and time fixed effects"
    - "The model includes reform dummies interacted with the Japan indicator, where Japan is TOPIX"
    - "Wild-bootstrap inference is clustered by country with 999 Rademacher bootstrap draws"
    - "The regression table is saved to output/tables/table2_ols.tex with booktabs rules and 2 decimal places"
  artifacts:
    - path: "src/analysis/panel_ols.py"
      provides: "Standalone two-way fixed effects panel OLS implementation"
      exports: ["construct_regression_panel", "fit_panel_specs", "main"]
    - path: "output/tables/table2_ols.tex"
      provides: "Paper-ready OLS table"
      contains: "\\toprule"
    - path: "output/tables/panel_ols_results.csv"
      provides: "Machine-readable PanelOLS and wild-bootstrap coefficient output"
  key_links:
    - from: "src/analysis/panel_ols.py"
      to: "linearmodels.PanelOLS"
      via: "PanelOLS(..., entity_effects=True, time_effects=True)"
      pattern: "PanelOLS"
    - from: "src/analysis/panel_ols.py"
      to: "wildboottest"
      via: "FWL demeaned statsmodels OLS with cluster country labels"
      pattern: "wildboottest"
    - from: "src/analysis/panel_ols.py"
      to: "output/tables/table2_ols.tex"
      via: "to_latex(..., float_format='%.2f')"
      pattern: "table2_ols\\.tex"
---

<objective>
Implement the primary two-way fixed effects panel OLS analysis.

Purpose: Estimate valuation responses to Japan reform events using `linearmodels.PanelOLS`, reform x Japan interactions, and country-clustered wild-bootstrap inference suitable for only four country clusters.

Output: `src/analysis/panel_ols.py`, `output/tables/table2_ols.tex`, and `output/tables/panel_ols_results.csv`.
</objective>

<execution_context>
@/Users/dandan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dandan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-CONTEXT.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-RESEARCH.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-VALIDATION.md
@/Users/dandan/Desktop/Projects/kor-discount/config.py
@/Users/dandan/Desktop/Projects/kor-discount/src/descriptive/table1.py
@/Users/dandan/Desktop/Projects/kor-discount/tests/test_phase3.py

<interfaces>
Required constants:
```python
BOOTSTRAP_ITERATIONS = 999
BOOTSTRAP_SEED = 42
WILD_BOOTSTRAP_WEIGHTS = "rademacher"
STUDY_END = pd.Timestamp("2024-12-31")
```

Required reform variables:
```text
post_stewardship
post_cgc
post_tse_pb_reform
is_japan
stewardship_x_japan
cgc_x_japan
tse_pb_reform_x_japan
```

Required public functions:
```python
def construct_regression_panel(panel: pd.DataFrame) -> pd.DataFrame: ...
def fit_panel_specs(reg_panel: pd.DataFrame) -> tuple[pd.DataFrame, pd.DataFrame]: ...
def main() -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PanelOLS specifications and wild-bootstrap inference</name>
  <files>src/analysis/panel_ols.py, output/tables/panel_ols_results.csv</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/panel_ols.py (file being modified or created)
    - /Users/dandan/Desktop/Projects/kor-discount/config.py (event date firewall)
    - /Users/dandan/Desktop/Projects/kor-discount/src/data/build_panel.py (panel schema)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-CONTEXT.md (D-05, D-06, D-07, D-08, D-15)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-RESEARCH.md (PanelOLS + FWL wildboottest workaround)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase3.py (OLS tests)
  </read_first>
  <behavior>
    - The script imports `PanelOLS` from `linearmodels` and uses it for point estimates.
    - The script sets a MultiIndex in the order `["country", "date"]`.
    - Every PanelOLS model uses `entity_effects=True` and `time_effects=True`.
    - The Japan indicator is `is_japan = (country == "TOPIX").astype(int)`.
    - Reform x Japan variables are named exactly `stewardship_x_japan`, `cgc_x_japan`, and `tse_pb_reform_x_japan`.
    - Wild-bootstrap inference uses `wildboottest`, `B=999`, `weights_type="rademacher"`, `bootstrap_type="11"`, `seed="42"`, and clusters by country.
    - The script writes `output/tables/panel_ols_results.csv`.
  </behavior>
  <action>
Create `src/analysis/panel_ols.py` as a standalone executable script. Use the project-root bootstrap pattern and import `config`, `PanelOLS`, `PanelData`, `statsmodels.api as sm`, and `wildboottest` directly. Do not import from `src.analysis.event_study` or `src.analysis.geo_risk`.

Define these exact constants:

```python
BOOTSTRAP_ITERATIONS = 999
BOOTSTRAP_SEED = 42
WILD_BOOTSTRAP_WEIGHTS = "rademacher"
STUDY_END = pd.Timestamp("2024-12-31")
```

Implement `construct_regression_panel(panel: pd.DataFrame) -> pd.DataFrame`:

1. Restrict to `date <= STUDY_END`.
2. Create `is_japan = (country == "TOPIX").astype(int)`.
3. Create `post_stewardship = (date >= pd.Timestamp(config.STEWARDSHIP_CODE_DATE)).astype(int)`.
4. Create `post_cgc = (date >= pd.Timestamp(config.CGC_DATE)).astype(int)`.
5. Create `post_tse_pb_reform = (date >= pd.Timestamp(config.TSE_PB_REFORM_DATE)).astype(int)`.
6. Create `stewardship_x_japan = post_stewardship * is_japan`.
7. Create `cgc_x_japan = post_cgc * is_japan`.
8. Create `tse_pb_reform_x_japan = post_tse_pb_reform * is_japan`.
9. Sort by `country`, `date` and set a MultiIndex with `["country", "date"]`.

Implement `fit_panel_specs(reg_panel: pd.DataFrame) -> tuple[pd.DataFrame, pd.DataFrame]` with three columns/specifications:

```text
Baseline two-way FE
+ reform dummies
+ reform x Japan
```

Use `PanelOLS(..., entity_effects=True, time_effects=True, drop_absorbed=True)` for point estimates. The common post-reform dummies are collinear with monthly time fixed effects; retain rows for `post_stewardship`, `post_cgc`, and `post_tse_pb_reform` in the output table with value `absorbed by time FE` when dropped. This preserves D-08's table structure while honoring D-05's two-way fixed effects.

For the `+ reform x Japan` specification, compute wild-bootstrap p-values for `stewardship_x_japan`, `cgc_x_japan`, and `tse_pb_reform_x_japan` using the FWL workaround:

1. Demean `pb` and the three interaction regressors with `PanelData(...).demean(group="both", return_panel=False)`.
2. Fit `sm.OLS(y_dm, x_dm).fit()` with no constant.
3. Pass the fitted statsmodels result to:

```python
wildboottest(
    sm_res,
    B=BOOTSTRAP_ITERATIONS,
    cluster=pd.Series(reg_panel.index.get_level_values("country"), name="country"),
    weights_type=WILD_BOOTSTRAP_WEIGHTS,
    bootstrap_type="11",
    seed=str(BOOTSTRAP_SEED),
    show=False,
)
```

Write `output/tables/panel_ols_results.csv` with columns `specification`, `term`, `coef`, `std_error`, `p_value`, `wild_p_value`, and `note`.
  </action>
  <acceptance_criteria>
    - `grep -q "from linearmodels import PanelOLS" src/analysis/panel_ols.py` exits 0.
    - `grep -q "entity_effects=True" src/analysis/panel_ols.py` exits 0.
    - `grep -q "time_effects=True" src/analysis/panel_ols.py` exits 0.
    - `grep -q "BOOTSTRAP_ITERATIONS = 999" src/analysis/panel_ols.py` exits 0.
    - `grep -q "weights_type=WILD_BOOTSTRAP_WEIGHTS" src/analysis/panel_ols.py` exits 0.
    - `grep -q "stewardship_x_japan" src/analysis/panel_ols.py` exits 0.
    - `python src/analysis/panel_ols.py` exits 0 and writes `output/tables/panel_ols_results.csv`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && python src/analysis/panel_ols.py && python -c "import pandas as pd; df = pd.read_csv('output/tables/panel_ols_results.csv'); assert {'specification','term','coef','std_error','p_value','wild_p_value','note'}.issubset(df.columns); assert 'stewardship_x_japan' in df['term'].values; print('panel csv OK')"</automated>
  </verify>
  <done>PanelOLS point estimates and wild-bootstrap inference run and write machine-readable OLS results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write booktabs Table 2 with 2 decimal places</name>
  <files>src/analysis/panel_ols.py, output/tables/table2_ols.tex</files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/src/analysis/panel_ols.py (file being modified)
    - /Users/dandan/Desktop/Projects/kor-discount/src/descriptive/table1.py (booktabs LaTeX output style)
    - /Users/dandan/Desktop/Projects/kor-discount/tests/test_phase3.py (table2 tests)
    - /Users/dandan/Desktop/Projects/kor-discount/.planning/phases/03-primary-empirics/03-CONTEXT.md (D-08 exact table output path and formatting)
  </read_first>
  <action>
Extend `src/analysis/panel_ols.py` with `write_latex_table(results_df: pd.DataFrame) -> None` and call it from `main()`.

The table output must:

1. Write exactly to `config.OUTPUT_DIR / "tables" / "table2_ols.tex"`.
2. Use the three specification columns `Baseline two-way FE`, `+ reform dummies`, and `+ reform x Japan`.
3. Include rows for `post_stewardship`, `post_cgc`, `post_tse_pb_reform`, `stewardship_x_japan`, `cgc_x_japan`, and `tse_pb_reform_x_japan`.
4. Format numeric coefficients and standard errors to exactly two decimal places.
5. Include booktabs rules by using `DataFrame.style.format(precision=2).to_latex(hrules=True, caption=..., label="tab:panel_ols")` or an equivalent pandas `to_latex` call that emits `\toprule`, `\midrule`, and `\bottomrule`.
6. Include a LaTeX table note containing the exact phrase `Wild-bootstrap p-values use 999 Rademacher draws clustered by country.`.
  </action>
  <acceptance_criteria>
    - `grep -q "table2_ols.tex" src/analysis/panel_ols.py` exits 0.
    - `python src/analysis/panel_ols.py` exits 0.
    - `test -s output/tables/table2_ols.tex` exits 0.
    - `grep -q "\\\\toprule" output/tables/table2_ols.tex` exits 0.
    - `grep -q "stewardship_x_japan" output/tables/table2_ols.tex` exits 0.
    - `grep -q "cgc_x_japan" output/tables/table2_ols.tex` exits 0.
    - `grep -q "tse_pb_reform_x_japan" output/tables/table2_ols.tex` exits 0.
    - `grep -q "Wild-bootstrap p-values use 999 Rademacher draws clustered by country." output/tables/table2_ols.tex` exits 0.
    - `pytest tests/test_phase3.py::test_table2_exists tests/test_phase3.py::test_table2_reform_dummies tests/test_phase3.py::test_table2_booktabs -x -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && python src/analysis/panel_ols.py && pytest tests/test_phase3.py::test_table2_exists tests/test_phase3.py::test_table2_reform_dummies tests/test_phase3.py::test_table2_booktabs -x -q</automated>
  </verify>
  <done>`output/tables/table2_ols.tex` is paper-ready, booktabs-formatted, and contains the reform x Japan interaction rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/processed -> panel OLS | `panel.parquet` is local processed data consumed by the OLS script. |
| script -> output/tables | The script writes CSV and LaTeX regression outputs under fixed project output paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | `panel.parquet` | mitigate | Read from `config.PROCESSED_DIR / "panel.parquet"` only and validate table outputs via tests. |
| T-03-03-02 | Repudiation | reform date construction | mitigate | Build post-reform dummies from `config.STEWARDSHIP_CODE_DATE`, `config.CGC_DATE`, and `config.TSE_PB_REFORM_DATE`; grep acceptance checks interaction names. |
| T-03-03-03 | Denial of Service | wild bootstrap | mitigate | Use fixed `BOOTSTRAP_ITERATIONS = 999`, fixed seed `42`, and no unbounded user-controlled iterations. |
| T-03-03-04 | Information Disclosure | regression outputs | accept | Outputs are aggregate public research tables with no PII or secrets. |
| T-03-03-05 | High severity appsec | panel OLS script | accept | No high-severity appsec threat applies: no network calls, no server, no auth boundary, no secret handling, and fixed local paths only. |
</threat_model>

<verification>
- `python src/analysis/panel_ols.py` exits 0.
- `pytest tests/test_phase3.py::test_table2_exists tests/test_phase3.py::test_table2_reform_dummies tests/test_phase3.py::test_table2_booktabs -x -q` exits 0.
- `grep -q "\\toprule" output/tables/table2_ols.tex` exits 0.
- `grep -q "Wild-bootstrap p-values use 999 Rademacher draws clustered by country." output/tables/table2_ols.tex` exits 0.
</verification>

<success_criteria>
- `output/tables/table2_ols.tex` exists, is non-empty, contains booktabs rules, and presents coefficients to two decimal places.
- `output/tables/panel_ols_results.csv` contains machine-readable coefficient rows and wild-bootstrap p-values.
- `src/analysis/panel_ols.py` uses `linearmodels.PanelOLS`, country and time fixed effects, reform x Japan interactions, and 999-draw Rademacher wild bootstrap clustered by country.
</success_criteria>

<output>
After completion, create `.planning/phases/03-primary-empirics/03-03-SUMMARY.md`.
</output>
