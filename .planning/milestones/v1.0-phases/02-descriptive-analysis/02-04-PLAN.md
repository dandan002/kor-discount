---
phase: 02-descriptive-analysis
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified: []
autonomous: false
requirements:
  - DESC-01
  - DESC-02
  - DESC-03

must_haves:
  truths:
    - "All 7 pytest tests in tests/test_descriptive.py pass (GREEN)"
    - "Figure 1 PDF opens correctly and shows 4 P/B series with 3 reform date annotations"
    - "Table 1 LaTeX fragment has booktabs formatting and all 4 sub-period rows"
    - "Discount numbers in discount_stats.csv match RESEARCH.md verified values within 0.05"
  artifacts:
    - path: "output/figures/figure1_pb_comparison.pdf"
      provides: "Visual confirmation of Korea Discount over 20-year period"
    - path: "output/tables/table1_summary_stats.tex"
      provides: "Compilable LaTeX table fragment with booktabs"
    - path: "output/tables/discount_stats.csv"
      provides: "Machine-readable discount statistics"
    - path: "output/tables/discount_stats.tex"
      provides: "Abstract-ready LaTeX newcommand fragment"
  key_links:
    - from: "pytest tests/"
      to: "all output artifacts"
      via: "tests/test_descriptive.py checks file existence and content"
      pattern: "pytest tests/test_descriptive.py -q"
---

<objective>
Run the full test suite, verify all outputs are correct, and get human sign-off on Figure 1.

Purpose: Confirm that all three Phase 2 scripts produced correct, publication-ready outputs before the phase is marked complete. The automated test suite proves structural correctness (file exists, booktabs present, t-stats significant). The human checkpoint confirms the visual quality of Figure 1 — something automation cannot assess.

Output: Phase 2 complete; outputs ready for Phase 3 consumption and paper assembly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dandan/Desktop/Projects/kor-discount/.planning/ROADMAP.md
@/Users/dandan/Desktop/Projects/kor-discount/.planning/phases/02-descriptive-analysis/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and log discount numbers</name>
  <files></files>
  <read_first>
    - /Users/dandan/Desktop/Projects/kor-discount/output/tables/discount_stats.csv (to confirm values match RESEARCH.md expected output)
  </read_first>
  <action>
Run the complete pytest suite and verify all outputs:

1. Run the full test suite:
   ```
   cd /Users/dandan/Desktop/Projects/kor-discount
   pytest tests/test_descriptive.py -v -q
   ```
   Expected: 7 passed, 0 failed, 0 errors.

2. If any tests fail, diagnose and fix the underlying script (do NOT skip tests or relax assertions):
   - "file not found" failures: rerun the corresponding script (e.g., `python src/descriptive/figure1.py`)
   - "toprule not found": table1.py used wrong to_latex call — must use `Styler.to_latex(hrules=True)`
   - "t-stat not significant": statsmodels import issue — ensure scipy==1.13.1 is installed
   - "TOPIX mean not negative": data restriction issue — confirm `df["date"] <= pd.Timestamp("2024-12-31")`

3. After all tests pass, print the discount summary for human review:
   ```python
   python -c "
   import pandas as pd, sys
   sys.path.insert(0, '.')
   import config
   df = pd.read_csv(config.OUTPUT_DIR / 'tables' / 'discount_stats.csv')
   print('=== Korea Discount Summary ===')
   for _, r in df.iterrows():
       print(f\"KOSPI vs {r['benchmark']}: {r['mean']:.3f}x P/B (t={r['t_stat']:.2f}, 95% CI [{r['ci_lower']:.3f}, {r['ci_upper']:.3f}])\")
   "
   ```

4. Verify expected values are within tolerance of RESEARCH.md pre-computed results:
   - KOSPI−TOPIX mean: expected ≈ −0.177 (acceptable range: −0.22 to −0.13)
   - KOSPI−TOPIX t-stat: expected ≈ −3.23 (acceptable range: |t| > 2.0)
   - KOSPI−MSCI_EM mean: expected ≈ −0.601 (acceptable range: −0.65 to −0.55)
   - KOSPI−MSCI_EM t-stat: expected ≈ −10.30 (acceptable range: |t| > 8.0)

   If values differ significantly from these ranges, the study period restriction (2024-12-31 cutoff) may not be applied correctly — check all three scripts.
  </action>
  <verify>
    <automated>cd /Users/dandan/Desktop/Projects/kor-discount && pytest tests/test_descriptive.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>
    - `pytest tests/test_descriptive.py -q` output ends with "7 passed" (no failures)
    - Discount summary printed to stdout showing negative means and significant t-statistics for both benchmarks
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three Phase 2 descriptive scripts have been implemented and verified:
    - src/descriptive/figure1.py → output/figures/figure1_pb_comparison.pdf (Figure 1: KOSPI P/B vs TOPIX, S&P 500, MSCI EM with 3 reform date annotations)
    - src/descriptive/table1.py → output/tables/table1_summary_stats.tex (Table 1: summary stats by country and 4 sub-periods)
    - src/descriptive/discount_stats.py → output/tables/discount_stats.{csv,tex} (Korea Discount magnitude with HAC t-stats)

    All 7 automated tests pass. Task 1 above printed the discount numbers — confirm they look reasonable.
  </what-built>
  <how-to-verify>
    1. Open `output/figures/figure1_pb_comparison.pdf` in a PDF viewer.
       Confirm:
       - 4 lines visible: KOSPI (blue), TOPIX (orange), S&P 500 (green), MSCI EM (red)
       - 3 vertical dashed grey lines at approximately 2014, 2015, and 2023
       - Each line has a text annotation (small grey text, rotated 90 degrees)
       - KOSPI line is visibly below TOPIX and MSCI EM for most of the period
       - X-axis shows years 2004 through 2024; Y-axis labeled "Price-to-Book Ratio (P/B)"

    2. Check the discount numbers printed by Task 1:
       - KOSPI vs TOPIX should be approximately −0.177x (negative, meaning Korea trades at a discount)
       - KOSPI vs MSCI_EM should be approximately −0.601x (negative)
       - Both t-statistics should have absolute value > 2.0

    3. Optionally scan output/tables/table1_summary_stats.tex — should open as readable LaTeX source with \toprule, \midrule, \bottomrule, and 4 sub-period groups visible.
  </how-to-verify>
  <resume-signal>Type "approved" if Figure 1 looks correct and discount numbers are reasonable. If issues found, describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner | pytest reads output files — local filesystem only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | test assertions | accept | Tests check structural properties (file existence, content substrings, sign of t-stat); minor numerical drift does not affect paper conclusions |
</threat_model>

<verification>
- `pytest tests/test_descriptive.py -q` — 7 passed, 0 failed
- Human confirms Figure 1 PDF is visually correct (4 series, 3 annotations, KOSPI below benchmarks)
- Discount numbers match RESEARCH.md expected values within stated tolerance
</verification>

<success_criteria>
- All 7 tests pass GREEN
- Figure 1 approved by human reviewer
- Korea Discount headline numbers ready for verbatim use: KOSPI −0.177x to TOPIX (t = −3.23), −0.601x to MSCI EM (t = −10.30)
- Phase 2 complete — phase status can be updated to "Complete" in ROADMAP.md and STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/02-descriptive-analysis/02-04-SUMMARY.md`
</output>
